# CURRENT_STATE — ALWTTT

This file tracks the currently validated project baseline, active work, and immediate next steps.

> **Recovery note (2026-06-12).** This file was accidentally overwritten by an insert-fragment ("Addendum para CURRENT_STATE.md §1") at some point after 2026-05-22 and was absent from project knowledge by 2026-06-01. The pre-overwrite version was recovered and the gap window (2026-05-22 → 2026-06-12) was replayed from chat-history insert blocks. Two batches in the window have **UNVERIFIED closure status** — see the `[GAP — UNVERIFIED]` stubs in §1. §1 convention going forward: new closure blocks insert at the TOP, newest-first.

> **Hygiene note (2026-06-16, DOC-HYGIENE).** §1 pruned per governance §18.8 ("CURRENT_STATE overgrowth") + invariant 7: the per-batch closed-work log that duplicated `changelog-ssot.md` was collapsed to a Foundation summary + a current-milestone closure table; the `[GAP — UNVERIFIED]` stub was preserved. §2 and §5 were also tidied (closed/resolved entries and the doc-closure history removed). §3 (S-sequence), §4 (open items + risks), and §6 are unchanged. `changelog-ssot.md` was left whole (D1=A). No operational reality changed; full history is in the changelog.

---

## 1. Project foundation

> Per-batch closed-work detail lives in `changelog-ssot.md` (kept whole — DOC-HYGIENE D1=A). This section is the durable foundation + the current-milestone closure list only (governance §18.8 / invariant 7).

### Foundation (pre-demo-cut) — each line is durable; the named SSoT owns it

- **Combat / gig loop** — Combat MVP (2026-03-23); unified Stress path (M4.1); Flow bifurcation + adaptive `LoopScoreCalculator` (M4.2); action-card double-discard, GigSettings 4-SO refactor, per-loop draw + per-loop inspiration hook + canonical `AddCurrentInspiration`, SongOrchestrator IOOR defense (M4.6F-1..4). Authority: `SSoT_Gig_Combat_Core.md`, `SSoT_Scoring_and_Meters.md`, `SSoT_Gig_Encounter.md`.
- **Composition / jam surface** — `CompositionSession` / `SongCompositionUI` / `SongConfigBuilder` / `MidiMusicManager`; loop-model simplification + per-track stem persistence + UI rework (Phase B B1). Authority: `SSoT_Runtime_CompositionSession_Integration.md` (+ boundary `SSoT_ALWTTT_MidiGenPlay_Boundary.md`).
- **Card system** — action + composition cards, effects, deck/hand pipeline; multiset decks (M4.4); per-musician starter auto-assembly (M4.6-prep); card-face type backgrounds + owner icon (S5b). Authority: `SSoT_Card_System.md`, `SSoT_Card_Authoring_Contracts.md`.
- **Status effects** — SO-based catalogue + container (M1.2); icon pipeline + animations (M1.8); Earworm audience status (M4.3); Indifference audience blocker + `ApplyIncomingVibe` canonical gate (B3); Captivated audience amplifier (R1). Legacy `StatusType` enum is migration coexistence. Authority: `SSoT_Status_Effects.md`.
- **Audience + reactions** — `ResolveLoopEffect` + 4-axis taste schema + macro-Vibe modifier ratified (S1); Cool Dude + Kid archetypes + Indifference (B3-content-audience). Authority: `SSoT_Audience_and_Reactions.md`.
- **Audio** — mix model + persisted balance (M-AUDIO-MIX); opt-in card SFX + scoped jitter (AUDIO-SFX-FIX); OST playback (AUDIO-OST); crowd ambience (AUDIO-AMBIENCE); per-character + per-ability SFX (AUDIO-CHAR-PROFILES 1/2); **bytes-plane mix gains** (BAL-1, MGP-MIX-1 on MidiGenPlay 1.2.0 — per-`(musicianId, TrackRole)` CC7 gain composed with the live plane; live re-assert now verified/deferred/composed). Authority: `SSoT_Audio.md` §4.6 + boundary `SSoT_ALWTTT_MidiGenPlay_Boundary.md` §8.3 (+ planning `Roadmap_Audio.md`).
- **Sensory layer** — typed event bus (S2); visual sensory migration + smoke/fire VFX (S3a); audio SFX layer (S3-audio). Shipped bus events: S2/S3 `AudienceReactionEvent`, `SongEndVibeEvent`, `SfxStageCrossedEvent`; S4 added `CardPlayedEvent`, `LoopResolvedEvent`, `StatusAppliedEvent`, `GigStartedEvent`, `GigOutcomeEvent`, `AudienceTurnStartedEvent` (semantic; consumed by the tutorial, add no SFX key); TUT-REBUILD added `MusicianStressHitEvent` + `AudienceBlockedEvent`; S5h added `RewardChoiceOpenedEvent` (→ `RewardOpened`); JUICE-PW added `AudienceVibeImpactEvent` (per-target card Vibe impact → `CardVibeImpact`; the first *card-effect* bus surface, and the first post-S4 event to add an SFX key). Tutorial runtime: `Assets/Scripts/Tutorial/`. Tutorial content is dual-catalog (EN + ES `TutorialDialogCatalogSO` assets, D-S5f-2=B); the demo runs the **Spanish** catalog (`.../Dialogs/ES/`), copy authored against the S5e inverted semantics; EN kept in parity (editor menu `ALWTTT/Tutorial/Validate catalog language parity`). Planning home: `Design_Sensory_Contract_v0_1.md`; audio home: `SSoT_Audio.md`.
- **Dev Mode** — Phases 1–3 (F12 tabs incl. Audio Mix). Authority: `SSoT_Dev_Mode.md`.
- **Editor authoring tools** — Card / Deck / Status Effect editors; per-musician starter UX; LLM-assisted card authoring (CE-L1). Authority: `SSoT_Editor_Authoring_Tools.md`, `SSoT_Card_Authoring_Contracts.md`.
- **Demo-cut prep** — auto-launch via `GigLauncher` + SFX→FlatVibe bonus (§5.3.5); polish layer feedback + animation (Phase B B2 / B2.5); B3 content (Sibi instrument identity, BPM / Modulation cards, audience pool); directional-modulation hint (ALWTTT-MOD-DIR-2/3). Planning home: `Design_Demo_Cut_v1.md`.
- **Planning reframe (2026-05-23)** — defined the demo-cut (S1-S5) + vertical-slice (S6-S8 = Phase C) sequence; locked D-TUT-1..5, D-RUN-1..6, and standing directives D1 (Sound Design Priority) / D2 (Sensory Contract) / D3 (Tutorial-as-mandatory). Detail: `changelog-ssot.md` (2026-05-23 entry), `Design_Tutorial_System_v0_1.md`, `Design_Vertical_Slice_v0_1.md`, `Design_Project_Directives_v0_1.md`.

### Demo-cut milestone — closed batches (newest-first; full detail in `changelog-ssot.md`)

| Batch | Closed | Outcome |
| --- | --- | --- |

| CTX-2a | 2026-08-03 | **Control de tempo en el tab Composition + default de tempo del modelo a `Slow` (D11=A).** Dos entregables. **(1) Producción:** el default de `PartEntry` pasa de `Very Fast`/`TempoRange.Fast` a `Slow` — cierra **F-TEMPO-1**, el defecto que hacía ilegibles los patrones de 8 compases de CONT-B y que, al no fijar tempo la carta de modo por defecto, era literalmente lo que sonaba en el demo. **(2) Dev Mode:** sección `Part tempo override (BPM)` sobre el patrón CTX-1 (stepper ± · Apply · Clear-con-restore · `Hold across loops`) más una línea de lectura del BPM resuelto. Cero API nueva. **El hallazgo del lote es de runtime, no de herramienta:** `CompositionSession` cortocircuita la resolución de BPM mientras `PartCache.resolvedBpm > 0`, y todas las invalidaciones de dev preservan ese valor (`keepTempo: true`), de modo que escribir el intent del modelo **no** cambiaba el BPM audible; Apply pone el caché a 0 y Clear reescribe el valor pre-Apply, lo que da restauración audible exacta sin depender de que el sorteo de banda sea reproducible. Precedencia verificada: `ExplicitBpm` ensombrece `TempoRange`, `TempoScale` multiplica después, y solo las cartas de tempo re-resuelven (`ShouldKeepTempo`) — por eso un override de 70 BPM + Push It da **105**, componiendo en vez de bloquear. `tempoScale` quedó descartado como palanca por ser el eje de gusto de la audiencia. **ST-CTX2A-1..7 PASS**; MidiGenPlay intacto. **Cierre parcial por decisión de usuario:** la pasada de medición para **D11b** (BPM legible + dispersión de `Slow` a lo largo de una canción) se difiere a **post-R3**; el lote entrega el instrumento, no el dato. **CTX-2b** (steppers de `chordExpression` / `arpeggioRate`) se verificó **inviable en este lote** y se abre como lote propio: esos campos no existen en el modelo — solo en el style bundle. Decisiones D11=A · D-CTX2-1=A · D-CTX2-2 (Hold reutilizado). Autoridad: `SSoT_Runtime_CompositionSession_Integration.md` §8 inv 13 + §12.4 · `SSoT_Dev_Mode.md` §18.13/§9.18. |

| CTX-2b | 2026-08-03 | **Override dev de articulación — `chordExpression` / `arpeggioRate` — en el tab Composition.** Precondición bloqueante resuelta: la expresión de acorde es **determinista** del style bundle (fila 14 de `Design_Composition_Debug_Tab_v0_1.md`). Plano nuevo, primero del tab que no escribe un campo del modelo: **clon en runtime del style bundle** (D-CTX2B-1=A), asset intacto, identidad de bytes recuperable en Clear vía `GetInstanceID` ⇒ clave de caché original. D-CTX2B-2=A: consumer-side puro, sin ask nuevo. ST-CTX2B-1..5+7 PASS; ST-CTX2B-6 diferido a test package-side. **Hallazgo del lote: F-ARTIC-RATE-RANDOM-1** (boundary §8.7), filtrado a MidiGenPlay como MGP-ARTIC-RATE-1. Autoridad: `SSoT_Dev_Mode.md` §18.14/§9.19. Live front sin cambios: **S5i → S5j**; siguiente enabler interleavable: **R3**. |

| R3 | 2026-08-08 | **Zig composition cards + continuidad de armonía compartida (JAM-1/JAM-2).** **Cerrado 2026-08-08; todas las verificaciones PASS** (ST-A1..A7 · ST-B1/B2 · ST-C1 · C5 · ST-R3-11 · C4 · ST-J1..J6). **Contenido:** catálogo Cantante con **Rise Up** (patrón verbatim de 8 compases por grado) y **Showtime** (ruta procedural; **operativa**, ST-R3-11 PASS), ambas StarterDeck 1 copia; paleta `Chord Palette - Modal` podada 7→5 (D-R3C-5=B); **tres cartas Wormus** (Test / Modal / Bossa) que existen con `flags=None` — banco de dev, invisibles para el jugador y ausentes del reward pool **por construcción** (D-R3C-6=A). **Runtime:** **JAM-1** (la armonía compartida se captura y se impone salvo que la tonalidad se haya movido o el bundle adopte) y **JAM-2** (el modo en que esa armonía realmente sonó viaja con ella; dos mapas separados a propósito — uno guarda el modelo y decide *si* imponer, otro guarda el render y decide *qué* propagar), ambos vivos en `CompositionSession`. **Arquetipo articulation-only verificado en juego:** misma progresión, 14 → 76 notas, timeline de acordes idéntico byte a byte. **Corrección de premisa:** D-R3C-3 pasa a **A′** — la decisión (la carta es el modo) sigue en pie, pero la adopción es de **ámbito render** y nunca alcanza el modelo de UI. **Nota de determinismo (F1):** los renders con seed pineado anteriores a MGP-MEL-1b **no son comparables** en la pista de melodía — relevante para la medición de D11b. **Baseline S5i:** segunda y tercera excepción autorizada al freeze (D-R3C-1=C, D-R3C-8=A). Autoridad: `SSoT_Runtime_CompositionSession_Integration.md` §13 · `SSoT_Card_Authoring_Contracts.md` §5.17/§5.18 · `SSoT_ALWTTT_MidiGenPlay_Boundary.md` §8.8. |

| LOG-1 | 2026-08-08 | **Higiene de consola + reparación de la etiqueta de acorde.** ST-LOG-1..8 todas PASS, incluidas las dos regresiones críticas: **ST-LOG-3** (las siete líneas protegidas siguen visibles con verbose OFF) y **ST-LOG-4** (JAM-2 sobrevive a la degradación). **Etiqueta de acorde reparada (D-LOG-1=B):** se reconstruye desde `deg`+`quality`, ambos ASCII por construcción; el glifo del marcador **nunca** se muestra, porque el original ya viene destruido y un `I7` de aspecto reparado significaría otro acorde que el `Imaj7` que suena. La tabla de sufijos acopla **por cadena**, no por el enum `ChordQuality` — decisión de frontera: ese enum es package-owned y append-only, y un switch por valor rompería el build ante un rename. **Niveles de log (D-LOG-3=B):** `LogVerbose` en `GigDevSettingsSO` + campo propio `logVerbose` en `MidiMusicManager`; `logDebug` pasa a `[HideInInspector]` porque el arranque lo sobreescribe y el mando no estaba conectado. Trazas de pila de `LogType.Log` desactivadas desde `AlwtttLogSetup` (762 de 864 líneas de la captura de referencia eran traza sin información); Warning y Error las conservan. **`[F-4]` retirado con el `try/catch` INTACTO y su volcado de error CONSERVADO** — se retiraron las líneas tageadas, no la defensa. **Huérfanos barridos** (D-LOG-2=B, solo los que R3 creó: `Chord Palette - Test` + 4 progresiones muertas → el resto sigue siendo CSV-6). **Tag `snapshot-01` cortado** (D-LOG-4=C, ordinal: el tag no describe, el changelog sí) ⇒ **R4+ DESBLOQUEADO** bajo D-SEQ-3=A. Dos asks nuevos a MidiGenPlay: **MGP-CHD-ASCII-1** y **MGP-LOG-VERBOSE-1**. Autoridad: `SSoT_Dev_Mode.md` §19 · `SSoT_ALWTTT_MidiGenPlay_Boundary.md` §8.9/§8.10 · `SSoT_Editor_Authoring_Tools.md` §17.12. |
| CONT-B (+ CTX-1/1b) | 2026-07-31 | **Pasada de contenido fase B — paletas de progresiones y percusión reconstruidas, validadas por escucha.** Ejecutada contra `MGP-20260729_Especificacion_Contenido_FaseA.md` (autoridad de contenido, package-side). **Progresiones:** 27 assets creados/sustituidos, paletas recableadas a **Core Major 13 · Core Minor 9 · Modal 7**, 16 assets viejos retirados, cero referencias colgantes. La paleta Modal pasa de 10 entradas no-modales a 7 assets modales reales (5 buques insignia con `tonalities` de un solo modo + 2 esqueletos DTP a 6 modos) — corrige el estado F-MODAL-SELFDEFEAT. **Percusión:** 8 patrones de 8 compases (rock, funk, jazz, hip-hop, latin son 3-2, metal, drum'n'bass, country train beat) con arquitectura 3+1/3+1, archivados en FourOnTheFloor (7→11) y SyncopatedPocket (1→5). **Bajos (package-side):** 5 ediciones de rango; los dos Slap salen de la banda subterránea C0–B1. **Catálogo consolidado** en `Patterns/{Chords,Drums}` — cero `OFF-ROOT` en assets locales, lo que probablemente **resuelve D-CSV-14 por construcción** (verificado indirectamente por T0.1: el dropdown de Backing del Dev Mode ya ofrece contenido local). **Validación:** 29 tests de escucha, **27 PASS · 1 FAIL (T4.5, F-KIT-1) · 1 parcial (T4.7, F-DNB-1)**; regresiones de paleta T4.9/T4.10 y de retirada T5.1/T5.2 todas PASS. **Código:** `CTX-1`/`CTX-1b` — override dev de tonalidad/raíz de parte en el tab Composition (§18.12 de `SSoT_Dev_Mode.md`). Desviaciones de la spec registradas para devolución: **D3** (columna Color §1.1) · **D5** (`tonalities` sin especificar en Core) · **D5b** (restricción SECDOM del Ragtime) · **D6** (décima entrada modal sin destino) · **D8** (rango de Slap) · **D9** (nivel Fingered vs Slap). Veredicto **A3 = CONFIRMADO** (el `iv7` Dominant7 es intencional y suena bien). Detalle en `changelog-ssot.md`. |
| R2 + R2c + R2d | 2026-07-31 | **Conito enablement + shared-harmony resolution + `InstrumentEffect.RandomFromList`.** Second interleavable enabler of the Roster Expansion campaign (D1=C), **out of the demo roster**. **R2 (authoring only):** Conito profile (`Bass` backing / `Guitar` lead → `InstrumentRules` routes `Bassline` off the backing list; 6-bass melodic whitelist for the dev pickers), two `InstrumentEffect` assets, three cards imported and catalog-registered in `Conito_CardCatalogData` (`starter_finger_bass` = `ArpeggioUp`/`PerBeat`/`ChordToneWalk`; `starter_slap_bass` = `Offbeat` + `SelfPocket`; `starter_static_rush` = `DrawCards(2)` then `ModifyStress(+1, Self)`), each `StarterDeck \| UnlockedByDefault \| copies 1`; the 10 legacy test entries **removed from the catalog, assets kept** (D-R2-8=B — diverges from R1's flags→`None` precedent, recorded not normalized). **The three MidiGenPlay bass-fidelity asks were resolved package-side (2026-07-28) before ALWTTT filed them** (boundary §8.4; incl. the naming trap `BassUpperSplit` ≠ `Bossa`). **R2c:** an ST-R2-1 FAIL root-caused to documented package behavior — a part with a Bassline row and no Backing row has **no harmony publisher**, so the bass renders silence by rule — led to wiring MidiGenPlay's `GenerateSinglePart(..., defaultProgression)` seam (SOLO-1) into `MidiMusicManager.RenderSinglePart`, sourced from a serialized `ChordProgressionPaletteSO` (**D-R2-6=B**, weighted pick, asset never mutated) seeded from `(songSeed, partIndex)` — stable within a song, varied across songs. Plus `InstrumentEffect.RandomFromList` (**D-R2-7**): append-only 4th mode + `melodicInstrumentPool`, resolved **once per card application** before the track loop (one coherent instrument for multi-track musicians) and persisted as an ordinary `overrideMelodicInstrument`, inheriting hash participation, cache coherence and supersession rules for free; `UnityEngine.Random` on purpose (a card play is not seed-reproducible); empty pool = warn + no-op. **R2d:** live gig exposed **F-BASS-ORDER-1** — playing the bass card *before* the backing card put the bass first in the track list, the package composed in list order, and the bass read the shared-progression cache before its only publisher wrote to it ⇒ **permanent silence for that part**. ALWTTT declined to fix it by reordering its own track list (list order feeds channel assignment and the instrument RNG ⇒ breaks byte identity; **track list order is consumer identity**, now a package contract) and filed **MGP-ALWTTT-BASS-ORDER-1**, delivered same-cycle: Backing-first composition passes with list-order merge, a rewritten guard that only discards the host default when the Backing row actually *carries* harmony (so articulation-only backing cards no longer suppress it), a normative five-level precedence, and a `sharedProgressionSource` readback. ALWTTT adopted all of it, retired its `!hasBacking` proxy, and replaced the cache token with a **pre-render shared-harmony identity** `dp:` + `bk:` (**D-R2-10=A**) — the package's suggested readback-based rule is not implementable, since the readback is produced by the render and the key decides whether a render happens. That `bk:` segment closed **F-HARM-STALE-1**, a latent defect since B1: swapping the Backing card changed the *Backing* track's hash but not the bass's, so the bass stem replayed from cache with the **old chords baked in** — silent wrong output, which is why it survived undetected. Also filed and delivered: **MGP-ALWTTT-BASS-SLAPFIG-1** → `PocketCouplingMode.SelfPocket`, autonomous slap/pop that reads no other track and therefore does **not** arm the §8.4 cache duty (`SlapPocket` still does); Slap Bass v1 re-authored onto it (**D-R2-11**), and `InstrumentEffect_SlapBass` moved to `RandomFromList {SB1, SB2}` (**D-R2-3** revised). Decisions D-R2-1..11. **ST-R2-2/4/5/6/7 · ST-R2c-1..6 · ST-R2d-1..7 all PASS**; ST-R2-1 superseded by ST-R2c-1; ST-R2-3 partial (multi-role-on-one-musician variant deferred to R5 Overload). Authority: `SSoT_ALWTTT_MidiGenPlay_Boundary.md` §8.4/§8.5/§8.6 · `SSoT_Runtime_CompositionSession_Integration.md` §8 inv 9 + §11 · `SSoT_Card_Authoring_Contracts.md` §5.13. Campaign home: `planning/active/RosterExpansion_Sub_Roadmap.md`. |
| BAL-1 | 2026-07-22 | **Consumer-side mix gains (bytes plane) adopted — MGP-MIX-1 on MidiGenPlay 1.2.0.** A second mix plane: per-`(musicianId, TrackRole)` **gain** baked as a CC7 at render time (`MixGainProfileSO`, content SO on `GigManager`, D-BAL-8=A), composing with the live per-musician axis. **Task 0 (live-plane collision):** the MPTK read corrected the boundary's original mechanism — F-MPTK-1 (MPTK resets channel CC7→100 every play; re-assert loop **required**, not retirable) + F-MPTK-2 (`OnEventStartPlayMidi` fires before tick-0, so the baked preamble is last-writer at start — the recorded race direction was inverted). Fix D-BAL-6=B + D-BAL-7: all musician-channel live writes route through one composing boundary `WriteChannelVolume01` (`live×baked×100/127`, identity idempotent) and the re-assert is deferred past tick-0 (`ReassertLiveMixAfterPreamble`). Gain folded into `ComputeHashFromTrackEntry` → stem/bundle keys (D-BAL-3=A, no stale CC7 on replay); `mixGains:` threaded into the per-part `GenerateSinglePart` (gig loop only); `appliedCc7ByTrack` + live-composed CC7 Dev strip. Decisions D-BAL-1=C/2=A/3=A/4=A/5=A (open) · 6=B/7/8=A (resolution). **ST-BAL-1..7 all PASS on 1.2.0** (byte identity ungained · gain=1.0 identity CC7=100 · gain=0 mute-without-delete · plane composition ~0.25 · start-race regression · Rhythm warn+ignore · cache replay). Anti-compensation rule normative. MidiGenPlay untouched. Authority: `SSoT_ALWTTT_MidiGenPlay_Boundary.md` §8.3 (flipped ADOPTED) · `SSoT_Audio.md` §4.2/§4.6 · `SSoT_Runtime_CompositionSession_Integration.md` §8 inv 9. |
| CSV-4 | 2026-07-20 (**partial**) | **Asset cleanup, package-baggage resolution and content-standard decisions — CSV-4 closes PARTIALLY.** Worklist A executed: **12 local orphans deleted** (6 chord progressions, 6 drum patterns, per sub-roadmap §4.1.1), counts verified against a fresh Export All, no live asset lost a reference and none gained `ORPHAN`. **5 local test style bundles deleted** (the two `Backing Card Config [roman]` override cards were test content, not starter — live backing content is Core Major + Core Minor only). One new local melody pattern authored (`Test_Scale_Melody_4-4_4m_14n`, in the correct plural `Patterns/Melodies` root). **MGP-BAGGAGE-1 filed and resolved package-side:** MidiGenPlay **1.1.0** adopted — 33 package assets retired, 8 moved to `Samples/ExampleCatalogue/`; consumer re-export verifies `EMPTY` / `NO-LANES` / `ALL-SILENT` / `OVERFLOW` at **zero** for all package-origin assets, and a standing rule was adopted that any reappearance is a package-side regression. Inventory re-baselined **230 → 218 → 183**; **the 230 and 218 export sets are superseded and void**. Decisions **D-CSV-7=A** (asset ownership is location-based; naming authority ≠ moving authority) · **D-CSV-15** (both melody mechanisms retained — `patternOverride` reserved for a future MIDI-import path) · **D-CSV-16=A** (card → bundle reverse index owed, now **blocking**: Modal/Test reachability could only be established by user statement, not tooling) · **D-CSV-18=A** (instrument curation is pool-level; all 79 instruments are package-owned) · **D-CSV-19=A** (renamer is a separate window; the inventory window stays read-only) · **D-CSV-21=C** (superseded by 23) · **D-CSV-22=B** (CSV-3 runs first) · **D-CSV-23=A+B** (8-measure progression standard for new/repaired content + Modal palette dissolved into Core Major/Minor by tonic; registered as CR-10, executes at **CSV-6**). Package-side **D-BAG-3 / MGP-MIX-1** (`volume01` / mix gain) registered as a **live open ask** with ALWTTT input delivered (per-musician granularity, multiplicative composition, CC7 not velocity); **BASSFILL-1 recalibrated** from demo blocker to robustness gap; **D-CSV-14 reduced** to Assets-side only. **Deferred out of the batch:** the listening pass (**blocked** — see §2) and the naming application (**CSV-4b**). Gig smoke pass green; no code change. Authority: `SSoT_ALWTTT_MidiGenPlay_Boundary.md` §4.3/§8.2 · `SSoT_Editor_Authoring_Tools.md` §17 · `SSoT_Runtime_CompositionSession_Integration.md` §12 · new `planning/Design_Asset_Naming_v0_1.md`. Arc home: `planning/active/CSV_Composition_Validation_Sub_Roadmap.md`. |
| CSV-1 + CSV-2 | 2026-07-18 | **Composition Inventory Window + dev instrument overrides — CSV arc opened and its tooling batch closed in one session.** **CSV-1:** `CompositionInventoryWindow` (`#if UNITY_EDITOR && ALWTTT_DEV`, D-CSV-10=A) — read-only browser over every composition asset family (style bundles · drum/chord/melody patterns · palettes · libraries · phrase archetypes · melodic + percussion instruments) with TS/source/text/orphan/dup/flagged/bundle-reachable filters, derived health flags (`BASS-GAP` is the static face of CR-7 — bass is single-pass, no repeat-to-fill), Print + Export JSON per view, and a Names Report feeding CSV-4. Reuses `PatternRepositoryResources` / `TrackPatternConfigStoreResources` / `InstrumentRepositoryResources`; mutates nothing (ST-CSV-7). **CSV-2:** per-track melodic + percussion override rows in the Composition tab (D-CSV-5=A refined) — writes `TrackEntry.override*Instrument` directly (already hash-participating, so no bypass machinery and no change to `SongCompositionUI`/`SongConfigBuilder`/`MidiMusicManager`), full catalogue for counterfactual probing with `(outside permitted set)` annotation, `[dev-inst]` intent marking, Clear-with-restore (byte-identical under pinned seed), and card-supersession detection. The one non-obvious piece: invalidation must mirror the instrument-**card** path (`keepInstruments: false`), not the pattern-stamp path, or a stale `resolvedMelInstByTrack` is re-fed as `instrumentOverrides`. **ST-CSV-1..8 PASS** (1 BC gate, 3 clear/restore regression, 8 production compile). MidiGenPlay untouched; no gameplay change. Decisions **D-CSV-5=A(refined)** · **D-CSV-6=A** · **D-CSV-10=A**; new open **D-CSV-11** (are `ChordProgressionData.Measures`/`TimeSignature` authored at all?). **Follow-ups CSV-1b + CSV-1c both closed 2026-07-18** — discovery unioned with `AssetDatabase`, reference harvest added, `OFF-ROOT`/`HARVESTED` flags and Export All shipped; chord progressions 13 → 48 and the orphan column is trustworthy. Carried forward: **D-CSV-13** (dev dropdown source) + **D-CSV-14** (Resources scan-root mismatch) + the CR-7 `BASS-GAP` measurement (27/30 live progressions). Worklist in sub-roadmap §4.1.1. Authority: `SSoT_Editor_Authoring_Tools.md` §17 + `SSoT_Dev_Mode.md` §18.9/§9.15 + `SSoT_Runtime_CompositionSession_Integration.md` §8 inv 9. Arc home: `planning/active/CSV_Composition_Validation_Sub_Roadmap.md` (planning-only). |
| DBG-C2 | 2026-07-17 | **Composition-debug interactive controls — the write half; MGP-ALWTTT-DBG arc CLOSED.** Made `patternOverrides` LIVE (D-C1-1's inert C1 passthrough now carries values): per-track override dropdowns (full registry, TS-filtered, off-band annotated — D-C2-2=A), a Roman field bridging `ChordProgressionRuntimeImporter.TryParseRoman` to a never-persisted Backing override with the importer verdict shown verbatim (D-C2-1=A), and R2a "re-render part now" via a stamp-bump through the normal seeded `PlaySinglePartLoop` (D-C2-3=A). Overrides are never cache-keyed: MMM bypasses stem/bundle caches when overrides are supplied and `PartCache` is stamp-invalidated on change (D-C2-4=A); idle ⇒ byte-identical (ST-C2-7). Bassline/Harmony vetoed in-UI. A1 CONFIRMED against `Design_Composition_Debug_Tab_v0_1 §3.1`. MidiGenPlay untouched; consumed only C1-recorded surfaces. Decisions D-C2-1..4=A; inherited D-C1-1/D-C1(seed)/D2/D3/D-DBG1..5/ID-1..4/E-1..5(+1b/2b). ST-C2-1..9 PASS (S7 BC gate, S8 clear/restore regression, S9 prod compile). **Follow-up:** DBG-OBS-1 (RenderOverride line may omit `pattern=`; non-blocking). Authority: `SSoT_Dev_Mode.md` §18.4–§18.8 + `SSoT_Runtime_CompositionSession_Integration.md` §8 inv 9. Next: arc-close `Roadmap_ALWTTT_Debug_Seams`. Detail in `changelog-ssot.md`. |
| DBG-C1 | 2026-07-17 | **MusicianTrackKey consumer migration + composition-debug read surface.** Consumer half (read side, D1=B) of the MGP-ALWTTT-DBG arc. Migrated ALWTTT to the package's composite `(musicianId, TrackRole)` keying **end-to-end** — stem/bundle/part caches, `RenderSinglePart` signature (`trackInputsHashByTrack`; trailing inert `patternOverrides`, D-C1-1=A), `PartCache`, `ComputeTrackInputsHashesForPart`, merged-rebuild channel ordering — retiring the `FlattenInstrumentReport` + id→key shims. **All three BASS-1 carve-outs retired** → multi-track musicians cacheable again (ST-S2: `cacheEnabled=True`, per-role hashes). Added a read-only render truth surface on `MidiMusicManager` (`LastResolvedByTrack`/serial; `GetChordTimelineSnapshot()` over the governed `chd:` marker) and `DevCompositionDebugTab` + `GenerationDebugFormatter` (`#if ALWTTT_DEV`: two-phase intent/resolved log, `'*'` convention [A1 pending], Compact/Full, Copy fingerprint, seed pin closing §8.7 debt) + an infinite composition-loop toggle (D2=A host hooks keep firing; CARD-UX-1 dev-exempt). BC gate verified (single-track byte-identical, ST-S1). MidiGenPlay untouched. Decisions D-C1-1=A · D-C1(seed)=A · D2=A · D3; inherited D-DBG1..5/ID-1..4/E-1..5. ST-S1..S10 PASS (S2 = BASS-1-retirement positive). **Open:** A1 (`'*'` vs `Design_Composition_Debug_Tab_v0_1 §3.1`, doc not in PK). Authority: `SSoT_Runtime_CompositionSession_Integration.md` §8 inv 9 + boundary §4.3 (BASS-1 RESOLVED). Next: **DBG-C2** (interactive controls). Detail in `changelog-ssot.md`. |
| DF-INSPLOOP (+ DEV-WINLOSE) | 2026-07-16 | **Card-gated per-loop Inspiration effect.** New `CardEffectSpec` subclass `AddInspirationPerLoopSpec` (JSON `"AddInspirationPerLoop"`, `amount ≥ 1`), the S5e-deferred `+INS` effect. Derived from `TrackEntry.sourceCardDefinition` at `CompositionSession.EvalPerLoopInsp` time — **track-scoped** (D-INSP-1=D): bonus lives while the carrying card's track is in the looping part, dies on same-role replacement. Single derivation point `AddInspirationPerLoopSpec.SumFor`; global `+INS` badge now shows the **total** per-loop gain (basal + derived), per-track badge shows the track's derived share. **Flat basal grant (currently 1/loop, `GigFlowSettings.asset`) untouched; `inspirationGenerated` stays 0 project-wide; `LoopTrackSnapshot`/`TotalComplexity` untouched → complexity term stays inert (D-INSP-4).** §9 four-layer conformant (runtime layer = track-binding; `CardBase` no-op branch). Import hardened: unresolved `styleBundle` path now warns instead of silently nulling. Two authored cards: **In the Pocket** (Track/Rhythm, C2) + **Vamp** (Track/Backing, Sibi), both cost 2, self-creating bundle, `RewardPool`+`UnlockedByDefault` in their **owning musician's catalog** (`C2_CardCatalogData` / `Sibi_CardCatalogData`) — D-INSP-6=A' (generic-catalog placement was runtime-ineffective; reward pool sources `RewardPool ∩ UnlockedByDefault` per-musician). **DEV-WINLOSE:** Dev overlay WIN/LOSE buttons exposing existing `GigManager` debug outcome paths (WIN opens RewardCanvas; outcome-tracker bypass preserved). Decisions D-INSP-1=D · 2=A · 3=A · 4 · 5=A · 6=A' · 7=A. ST-1..9 PASS (ST-4 after bundle assign; ST-7 by-construction; ST-9 after A' re-author). Detail in `changelog-ssot.md`. |
| BASS-1 + BASS-CARD-1 | 2026-07-12 | **Multi-role tracks per musician.** A part's tracks are now keyed `(musicianId, role)`, not `musicianId` — one musician may hold Backing + Melody + Bassline simultaneously. Same-role card replaces; different-role card adds. Ripple applied across `SongCompositionUI`, `SongPartElementUI`, `SongConfigBuilder`, `CompositionSession`, `MidiMusicManager` (UI row registry, instrument pins, stem-cache keys, `(musicianId, role)` channel stamping). Decisions D1–D4 = A. **Fixed a live starter-deck bug** (not only the bass blocker): Sibi's Wormus (Backing) → Singing Field (Melody) previously retargeted her Backing track, destroying the song's harmony and the shared-progression mechanic the starter deck is designed around. **BASS-CARD-1** closed the authoring gap: `composition.trackAction.styleBundleCreate` mints and configures a role-typed style bundle from JSON (Bassline cards were unauthorable — a `BasslineCardConfigSO` carries articulation, not a palette, so there was nothing to point at); banned from LLM output; Bassline added to the LLM role-hint list. MidiGenPlay untouched. ST-BASS-1..9 **all PASS**. Authority: `SSoT_Runtime_CompositionSession_Integration.md` §11 + §8 inv. 9/10. |
| S5e | 2026-07-02 | Core-semantics inversion (mirror of Cohesion): Stress → depleting mental-fortitude pool (0=Breakdown); Vibe → depleting persuasion-resistance pool (`CurrentVibe`/`MaxVibe`, 0=Convinced); `VibeGoal` retired into `MaxVibe` (convince = `Vibe ≤ 0`); magnitudes / Composure / Exposed numerically unchanged. Inspiration → fixed 3/loop + card costs (D2); `inspirationGenerated` content-deprecated, stripped to 0 on all 36 CardDefinition assets (11 changed) (D3); `+INS` CardEffect deferred (delivered DF-INSPLOOP, 2026-07-16). LoopScore complexity term inert (D-S5e-1=A, locked). Docs: Scoring §2.3/§3.2/§5/§6/§7.3, Audience §2–§5.2/§10/§11, Card Authoring inspiration fields (+ C=B consistency sweep). **S5e-ext** (same close): meter-bar hidden-if-full / visible-if-damaged + hover-reveal; card gen-badge auto-hide at gen=0; `PersistentGameplayData` musician-seed fix (Current seeds at `MaxStress`, not 0). 10/10 S5e + 7/7 S5e-ext smoke PASS. Detail in `changelog-ssot.md`. |
| S5a | 2026-06-22 | Vibe delivery + transparency. SFX stage bonus no longer applied mid-song: `ApplySfxBonusVibe` banks into a song-scoped `_pendingSfxVibe`, paid once at song end in `RunSongVibeResolution` after the Flow multiply (L-part only; SFX flat, D-S5-SFX-SCALE=A) — total unchanged vs pre-S5a (regression-verified), only the timing moved (D-S5-VIBE=B; no double-apply). Transparency surfaces (planning home `Design_Vibe_Telegraph_v0_1.md`): C1 `L+SFX` readout under the SongHype bar (`GigCanvas.SetVibeReadout`), C2 per-enemy effectiveness + C3 projected number (`AudienceCharacterCanvas.SetVibeTelegraph` + `VibeEffectiveness`), fed by `GigManager.GetLiveAvgImpression` at loop boundaries (D-S5-TELEGRAPH-SCOPE=B all three; D1=A single combined cyan floater). `[S5a-SMOKE]` log family added for verification. **All ST-S5a-1..9 PASS** (ST-5/6/7 confirmed via Dev Mode: indifference pre-song, `isTall` front-member, Stats-tab SongHype slider for the SFX-only edge; ST-8 via song-2 `SONG-START pendingVibe=0`); a real-Flow run (flowStacks=2) confirmed the Flow×L-only invariant (L Flow-boosted, flat SFX unscaled). |
| S5b | 2026-06-20 | Card clarity + animation. Card-face type backgrounds (`actionBackground`/`compositionBackground` toggled in `CardBase.SetCard` by domain) + owner icon (`ownerIconImage` ← `GigManager.TryGetMusicianIcon` → `MusicianCharacterData.CharacterIcon`, in-gig band only, no-icon fallback). Play-animation gated to musicians with a track in the played part (`CompositionSession.GetMusicianIdsWithTrackInPart` + `CharacterAnimator` master gate); non-performers settle to rest pose in-loop, `ResetStageToIdle` re-enables idle sway between songs (D-S5b-IDLE). Dev W/L tally (`DevGigOutcomeTracker`, normal-flow `GigOutcomeEvent` only) for S5c. Smoke ST-S5b-1..9 PASS (ST-3b deferred — no neither-domain card in demo deck). D1/D2/D3 = A. |
| S4 | 2026-06-17 | First-time tutorial controller + 6-beat guided jam over the 1st song. Bus +6 semantic events (CardPlayed/LoopResolved/StatusApplied/GigStarted/GigOutcome/AudienceTurnStarted). Overlay = hand-written UI cutout shader spotlight + captain bubble. Single-modal priority queue + gameplay gate; persisted firedDialogs (in-memory across scene loads); revisit/reset API. Fixed a tutorial double-show (D-S4-DEDUP=B). Smoke suite complete. |
| TUT-JAM-SEQ | 2026-06-16 | Jam taught as a 6-beat guided sequence over the 1st song (design + doc; D-TUT-6..11). Implementation realized by S4. |
| AUDIO-CHAR-PROFILES-2 | 2026-06-16 | Per-ability audience SFX (inline on `AudienceAbilityData`). Also records phase-1 (per-character reaction SFX) closure. |
| AUDIO-AMBIENCE | 2026-06-16 | Looping crowd ambience (SFX group); ducks under a performing song, returns at song end. |
| AUDIO-OST | 2026-06-16 | OST music bus (`MusicDirector` + `OstCatalogSO`); Main Menu theme; never overlaps gig music. |
| AUDIO-SFX-FIX | 2026-06-15 | Opt-in-by-type card SFX; app-wide SFX level (UI under SFX); caller-controlled jitter. |
| M-AUDIO-MIX | 2026-06-15 | Dev "Audio Mix" tab + persisted balance; created `SSoT_Audio.md`. |
| S3-audio | 2026-06-14 | Audio SFX layer (`SoundBankSO` + `SensorySfxType` + `SensoryAudioAdapter`; `SfxStageCrossedEvent`). Placeholder clips. Closed the S3 umbrella. |
| S3a | 2026-06-14 | Visual sensory polish; bus is the sole FT source; smoke/fire VFX; Kid "Tantrum" animator restored. |
| S2 | 2026-06-14 | Sensory Event Bus (2 event types) + `SensoryFxAdapter`. TutorialController deferred to S4 (D-S2-5). |
| CE-L1 | 2026-06-11 (docs 06-12) | LLM-assisted card authoring. Cross-project tooling interlude, outside the S1–S8 numbering. |
| S1 | 2026-06-12 | Audience reactions ratified (`ResolveLoopEffect` + taste schema + macro-Vibe) + neutral "…" FT. |

### ALWTTT-PCE-PROP — drum-palette propagation (RESOLVED 2026-07-06, S5g close)

Fix applied: `starter_default_mode_Payload_Rhythm_StyleBundle.patternPalette`
SyncopatedPocket → FourOnTheFloor (`patternOverride` verified null). Final bindings
in `SSoT_Card_System.md` §5.2.1. PCE-PROP ST-1..5 subsumed by ST-S5g-1..5
(**all PASS 2026-07-06**). D-TEMPO=null recorded (PCE §6 Option A confirmed).
**Reconciliation:** PCE-PROP D3=A ("determinism = deterministic per build,
package-threaded seed") is superseded in spirit by the seed-variety policy
(Integration SSoT §10, MGP-ALWTTT-SEED-1): the seed exists for cross-song variety
with intra-song stability, not per-build reproducibility.

## 2. Active work

- **Escucha de validación de contenido — EJECUTADA 2026-07-31 (CONT-B).** Desbloquea D-CSV-18 (`volume01`): captura entregada — los bajos se distinguen entre sí y los slaps leen; el único desequilibrio medido es Fingered por debajo de Slap, y **no es de instrumento sino de payload** (los boosts de Pocket son exclusivos de slap/pop). Ver **D9** (§6.2).

- **Card-authoring flow consolidation — LIVE (AUTH-1, closed 2026-07-31).**
  Editor-only; the build is byte-identical. New `PartEffectEditorWindow`
  (PartEffect family: list/filter/search, Create in `_PartEffects/`, Duplicate,
  Delete with usage scan, inline inspector, Find Usages, Export JSON carrying a
  card → PartEffect reverse index; TypeCache-driven, so MeterEffect /
  TempoEffect / TonalityEffect and any future subtype are included
  automatically — 30 family assets discovered at close). New mode-conditional
  `InstrumentEffectEditor` custom inspector (D-AUTH1-4=A; presentation-only,
  applies in the standard Inspector too). Shared `CardAuthoringNav` strip across
  the four card windows plus context-carrying Edit links. `CardInventoryWindow`
  gains a detailed print (per-card parameters: Action timing + effects with
  amounts/targets; Composition primaryKind, track role + bundle + bundle-field
  dump, part action, modifiers with scope/timing/label; entry flags/copies/
  unlockId) — Detailed-OFF print and JSON export byte-preserved. Decisions
  **D-AUTH1-0..4**; the family was relabelled at open because **D-AUTH-1/D-AUTH-2
  are the S5g music-variety locks**. MC-AUTH1-1..10 + MC-AUTH1b-1..9 PASS.
  Born from R2/Conito authoring friction. Authority:
  `SSoT_Editor_Authoring_Tools.md` §4.11 / §5.8 / §8.7 / §13 / §18 / §19.
  JSON/LLM import contract, runtime, and shared `test_*` / `MVP_*` assets
  untouched.

The live active-work stream is the demo-cut sequence in §3 (strict left-to-right; one batch open at a time). **S5f closed 2026-07-04. S5g closed 2026-07-06 (music variety — D-AUTH-1=A/D-AUTH-2=B; 20 DrumPatternData → 6 entries × 4 palettes; PhrasePalette_SingingField ×5 bound to Singing Field; Default Mode palette fix; ST-S5g-1..5 PASS; card→palette bindings in `SSoT_Card_System.md` §5.2.1; `ALWTTT-PCE-PROP` resolved — §1). ECON-1 closed 2026-07-07 (per-turn play economy — inserted between S5g and S5h; see the operational block below and `SSoT_Gig_Combat_Core.md` §14). TUT-REBUILD closed 2026-07-10 (guided gig-1 tutorial — an inserted arc between ECON-1 and S5h, batches TUT-R1/R2/R2b/R2c/R3; see the operational block below and `planning/active/TUT-REBUILD_Sub_Roadmap.md`). BASS-1 + BASS-CARD-1 closed 2026-07-12 (multi-role tracks per musician + Bassline card authoring — an inserted cross-cutting fix during S5h; see the operational block below). CARD-UX-1 closed 2026-07-13 (unplayable-card overlay + single playability source + final-loop composition lock + spawn-hook tutorial highlights — an inserted demo-cut-close batch during S5h; see the operational block below and `SSoT_Card_System.md` §10.5). S5h closed (**code 2026-07-07** — reward screen + #6b-lite venue-SFX unlock; **doc pass applied 2026-07-13**, S5h-DOC, after being blocked on this file's absence from the PK; see the operational block below). JUICE-PW closed 2026-07-13 (Psychic Waves / card Vibe-impact presentation on the sensory bus — an inserted demo-cut-close batch; see the operational block below). **DEMO-FIXES-A closed 2026-07-15** (DEMO-TUT-TOGGLE gig-open opt-in + R1 beat-8 `HandHas` + CT1 persistent pulse + DF-COST0 cost-badge hide + DF-ECONTIP pip tooltip + DF-CATALOG runtime band-catalog union; D-DF-1..8=A; ST-DF-1..6+8..13 PASS, ST-DF-7 deferred to Dev Mode; see the sub-roadmap + `DEMO-FIXES-A_Doc_Diffs_2026-07-15.md`). Remaining in DEMO-FIXES: **DF-INSPLOOP** (new `+INSP/loop` effect spec — own batch, ideally before S5i) and **DF-ARTIC** (cross-boundary). Next: **S5i** (playtest / tuning).** The tester-driven REPLANNING session closed 2026-07-01; it clustered the first-tester findings and locked a pre-tuning sub-sequence — S5e → S5f (Spanish onboarding + first-gig shape) → S5g (music variety) → ECON-1 (play economy) → S5h (reward screen) → S5i (win-rate; was S5c) → S5j (close; was S5d), ledger D-REPLAN-1..6, batches in `planning/active/S5_DemoCutClose_Sub_Roadmap.md`. S5a + S5b + S5e + S5f closed (S5e 2026-07-02, S5f 2026-07-04); S4 (tutorial + TUT-JAM-SEQ jam sequence) closed 2026-06-17. The historical per-batch "active work" entries (M1.3, Phase B B1/B2/B3, M4.3, Dev Mode Phase 3, Deck Editor, editor-tooling docs) were removed in the 2026-06-16 hygiene prune — all closed; detail in `changelog-ssot.md`. **BALANCE-XREF closed 2026-07-16** (documentation-only: deckbuilder balance-research cross-reference → new maxims doc `planning/Design_Game_And_Card_Maxims_v0_1.md`, decisions BR-D1..4, S5i observation lenses; no gameplay change). It **opened TLM-1** (an `ALWTTT_DEV`-gated run telemetry logger, BR-D2=A). **TLM-1 closed 2026-07-16** (`DevRunTelemetryLogger` — one JSONL record per gig on `GigOutcomeEvent`: outcome/lossCause, songs/loops, per-audience end-Vibe, plays with song-index-at-play-time [confound guard], per-card counts, roster + encounter label; D-TLM-1=A jsonl / D-TLM-2=B field set / D-TLM-3=A cohesion-loss gap accepted+documented, optional rider TLM-1b; ST-TLM-1..4 + R1 PASS; surface in `SSoT_Dev_Mode.md` §17). The immediate live-work order is now **S5i → S5j**.

- **Articulatory singer voice — LIVE and demonstrable (SINGER-1, closed 2026-07-21).** The singer character (Zig, `musicianId="3"`) audibly **sings** the melody the player composes — live, one voice, Settled profile — while the rest of the band plays underneath through MPTK; a melody card played mid-gig is sung on the **next loop**. It sings a musician's `Melody`/`Lead` **stem** (already returned by `RenderSinglePart`) and mutes that one channel via `MidiMusicManager.SetChannelVolume`; an unmatched melody plays as ordinary GM MIDI, so coexistence is by construction. Transport is a dsp anchor at `OnSongStarted` (D1=A; offset constant to one DSP buffer, per-profile `startTrimMs`, Zig +20). Budget 1 active / 2 hard ceiling. Exactly one `[SINGER-1]` seam on `CompositionSession` (`LoopPlaybackStarting` before `PlayRaw`); `MidiMusicManager` and MidiGenPlay unchanged. Consumer-side Pink Trombone fork at `Assets/ThirdParty/PinkTrombone/` (MIT, POC-FORK 1–7), never in `MidiGenPlay.Runtime`. ST-V1..V8 PASS. Governed by `systems/SSoT_Singer_Voice.md`. **Next candidates:** Zig voice-character pass (C), then the D4=A `VoiceProfileSO` field on `MusicianCharacterData` (B). **Deferred to Dev Mode:** `MidiMusicManager.Highlight` × mute interaction; cap=2 second-voice validation (Zig self-harmony finisher is the intended first consumer of slot 2). **Follow-up:** singer mixer-group routing (currently bypasses `AudioMixSettings`; balance is the profile `gain`).

- **R2a card debug-play + resolved meter/tonality/root read line — LIVE (CSV-3, closed 2026-07-22).** Composition Dev tab (`#if ALWTTT_DEV`, production byte-identical) gains `DevInjectCompositionCard` — auditions any catalogue card's **musical side only** (skips cost + `CardPayload.effects`), on the **live model** (D-CSV-8=A, reuses the seeded `PlaySinglePartLoop`) and **economy-neutral** (D-CSV-24=B — injected tracks excluded from `EvalPerLoopInsp`, reclaimed by a genuine play, cleared at song boundary), plus a resolved meter/tonality/root read line (`LastRenderResolved*`, sibling of BAL-1 CC7) and an overlay outer-scroll fix. **D-CSV-13=A** (Backing dropdown stays repository-fed + notice). **The melody-path finding is CLOSED as not-a-bug** — meter collision by construction (Core Minor holds zero 6/8 progressions); runs A/B no divergence; ST-CSV3-6 confirmed C2a healthy (Core Minor aligns to Aeolian); **no package ask** (`MGP-ALWTTT-MEL-ORDER-1` not filed) — so the **CSV-4 listening pass is UNBLOCKED**. Ownership resolved **D-MEL-1=A** (rhythm card carries the meter via a `MeterEffect`). ST-CSV3-1..9 + 5b/5c PASS. Homes: `SSoT_Dev_Mode.md` §18.1/§18.6/§18.10/§18.11/§9.16 · `SSoT_Runtime_CompositionSession_Integration.md` §8 inv 9 / §12 · `SSoT_Card_Authoring_Contracts.md` §5.16 · boundary §4.3. Doc-pass batch: **CSV-3-DOC**.

- **Composition asset baseline is 183 — CSV-4 partially closed 2026-07-20; the listening pass is UNBLOCKED (CSV-3, 2026-07-22).** Content and cross-boundary batch of the **CSV** arc; no code change, no gameplay change. **Current inventory truth is the 183-asset export** (post-MidiGenPlay-1.1.0). The **230- and 218-asset export sets are superseded and must not be cited**: 230 was CSV-1c's corrected baseline, 218 was post-worklist-A, and 183 is post-package-cleanup. ALWTTT deleted 12 local orphan assets and 5 local test style bundles; MidiGenPlay retired 33 assets and moved 8 into `Samples/ExampleCatalogue/` in response to **MGP-BAGGAGE-1** (`SSoT_ALWTTT_MidiGenPlay_Boundary.md` §8.2). **Reachability corrections:** live backing content is **14 of 33** progressions (Core Major 8 + Core Minor 6; `Chord Palette - Test` is now orphan and its 4 are dead; Modal's 10 exist but no card references them), and the live drum set is **26, not 27** (`DNB 4-4 2m test` hung off the deleted package palette). **That reachability rests on the user's statement, not on tooling** — the window still has no card → bundle reverse index, which is why **D-CSV-16** escalated from *nice to have* to *blocking*. The `Samples/` move did **not** reduce the consumer inventory count, and that is **expected, not a regression**: `CompositionInventoryWindow` discovers via `AssetDatabase`, deliberately broader than `Resources`; a third `sample` origin is specified for CSV-4b (`SSoT_Editor_Authoring_Tools.md` §17.6). **The former blocking finding — melody follows neither the meter nor the scale — is now CLOSED as not-a-bug (CSV-3, 2026-07-22).** The 6/8 + Core Minor + Singing Field observation was a **meter collision by construction** — Core Minor holds **zero** 6/8 progressions; runs A/B showed no divergence and ST-CSV3-6 confirmed the melody aligns to Aeolian when tonalities are authored. Ownership is assigned **D-MEL-1=A** (a rhythm card carries a matching `MeterEffect`; a part's meter is a model-construction `FourFour` default mutated only by a `MeterEffect`). **No package ask** (`MGP-ALWTTT-MEL-ORDER-1` not filed). Consequence: **the listening pass is now open** (worklist D). Recorded ALWTTT-side in `SSoT_Runtime_CompositionSession_Integration.md` §12 (rewritten CLOSED) and `SSoT_Card_Authoring_Contracts.md` §5.16. Also locked here: instrument curation is **pool-level** (D-CSV-18=A — all 79 instruments are package-owned, so verdicts edit `InstrumentRules` and the per-musician whitelists, never the assets), the **8-measure chord content standard + Modal-palette merge** (D-CSV-23=A+B, CR-10, executes at CSV-6), and the naming convention draft (`planning/Design_Asset_Naming_v0_1.md`, applied at **CSV-4b**).

- **Composition inventory + dev instrument overrides — LIVE (CSV-1 + CSV-2, closed 2026-07-18).** Opening batch of the **CSV** arc (Composition Session Validation, `planning/active/CSV_Composition_Validation_Sub_Roadmap.md` — planning-only). Tooling only: no gameplay change, no content change, MidiGenPlay untouched, and the arc runs **parallel to / behind S5i**, which remains the live front. `CompositionInventoryWindow` (editor, `#if UNITY_EDITOR && ALWTTT_DEV`) is the first structured view of the composition asset inventory in either project — seven views, filters, derived health flags, Print + Export JSON, plus a Names Report that is the CSV-4 naming input. The Composition tab gained per-track melodic/percussion instrument overrides that write the model's existing `TrackEntry.override*Instrument` fields (D-CSV-5=A: those GUIDs are already `trackInputsHash` inputs, so the cache stays coherent without any bypass machinery, and no production file changed); invalidation mirrors the instrument-card path (`keepInstruments: false`) rather than the pattern-stamp path. Clear restores the pre-dev field state byte-identically under a pinned seed; a later `InstrumentEffect` card supersedes a dev override by design and is detected and reported. ST-CSV-1..8 PASS. Primary homes: `SSoT_Editor_Authoring_Tools.md` §17 · `SSoT_Dev_Mode.md` §18.9 · `SSoT_Runtime_CompositionSession_Integration.md` §8 inv 9. **CSV-1b + CSV-1c closed same day** (discovery unioned with `AssetDatabase` + reference harvest + `OFF-ROOT`/`HARVESTED` flags + Export All): chord progressions 13 → **48**, orphan column now trustworthy, D-CSV-11 dismissed (the fields *are* authored — the zeros were a dead-set artifact). **Two findings carried forward:** all 30 live chord progressions are `OFF-ROOT`, i.e. `PatternRepositoryResources` resolves none of the content the game plays, so the §18.6 dev Backing dropdown offers only dead assets (**D-CSV-13**, CSV-3) and the scan roots themselves are mismatched (**D-CSV-14**, CSV-5, cross-boundary); and `BASS-GAP` — now a valid signal — fires on **27 of 30** live progressions, the strongest pre-CSV-5 evidence yet for `MGP-ALWTTT-BASSFILL-1`. Clean-slate worklist (12 local deletions, 2 design calls, 28 package-baggage items) in sub-roadmap §4.1.1; bundle cleanup is **not** decidable from this export (no card → bundle index, `SSoT_Editor_Authoring_Tools §17.10`).

- **Composition-debug interactive controls — LIVE (DBG-C2, closed 2026-07-17; MGP-ALWTTT-DBG arc CLOSED).** The write half of the composition-debug tab. `patternOverrides` is now LIVE: per-track override dropdowns sourced from the full runtime pattern registry (TS-filtered, off-band assets annotated), a Roman free-text field routed through `ChordProgressionRuntimeImporter.TryParseRoman` into a never-persisted Backing override (importer verdict verbatim; out-of-alphabet ⇒ hard fail, nothing applied), and an R2a "re-render part now" that bumps an override stamp to force a fresh render through the normal seeded `PlaySinglePartLoop`. Overrides are never cache-keyed — `MidiMusicManager` bypasses stem/bundle caches when overrides are supplied and `CompositionSession` invalidates `PartCache` on stamp mismatch; idle controls ⇒ byte-identical (ST-C2-7). Bassline (shared progression, warn+ignore) and Harmony (no v1 channel) are vetoed in the UI. A1 confirmed. MidiGenPlay untouched. Primary homes: `SSoT_Dev_Mode.md` §18.4–§18.8 · `SSoT_Runtime_CompositionSession_Integration.md` §8 inv 9. **Follow-up:** DBG-OBS-1 (RenderOverride resolved line may omit `pattern=`; override correctness unaffected). **Arc close:** `Roadmap_ALWTTT_Debug_Seams` (written as the arc-close deliverable).

- **MusicianTrackKey migration + composition-debug read surface — LIVE (DBG-C1, closed 2026-07-17).** Consumer read-side half of the MGP-ALWTTT-DBG arc. ALWTTT now consumes the package's composite `(musicianId, TrackRole)` keying across the stem/bundle/part caches, `RenderSinglePart`, and `ComputeTrackInputsHashesForPart`; the id→key expansion + `FlattenInstrumentReport` shims and all three BASS-1 carve-outs are gone (multi-track musicians cacheable — ST-S2). New `#if ALWTTT_DEV` Composition tab surfaces the package's `resolvedByTrack` per track (two-phase intent/resolved, `'*'` resolved-only convention, Compact/Full, Copy fingerprint) plus a `chd:` timeline dump (via the new production `MidiMusicManager.GetChordTimelineSnapshot()`), a seed pin (closes the §8.7 Dev-Mode debt), and an infinite composition-loop toggle (countdown resets, per-loop draw/inspiration keep firing — D2=A; `IsFinalLoopRunning` dev-exempted so CARD-UX-1 doesn't misfire). MidiGenPlay untouched; BC gate verified. Primary homes: `SSoT_Runtime_CompositionSession_Integration.md` §8 inv 9 (+ inv 11 dev rider) · `SSoT_ALWTTT_MidiGenPlay_Boundary.md` §4.3 (BASS-1 RESOLVED) · `SSoT_Dev_Mode.md` §18. **Open:** A1 (`'*'` convention vs `Design_Composition_Debug_Tab_v0_1 §3.1` — design doc absent from PK). **Superseded-forward by DBG-C2 (2026-07-17):** interactive controls landed; see the DBG-C2 bullet.

- **Card Vibe-impact presentation — LIVE (JUICE-PW, closed 2026-07-13).** An inserted demo-cut-close batch, not an S-sequence batch. A card's `ModifyVibe` effect now produces a sensory artifact at **effect resolution**: `CardBase.ExecuteEffects` publishes **`AudienceVibeImpactEvent` per audience target** (audience + performer + card + base/final/applied delta + `FanoutIndex`/`TargetCount`); `SensoryFxAdapter` spawns a staggered per-member floater (`-N` cyan, or grey `INDIFFERENT` when Indifference gated it) and fires `CharacterAnimator.PlayImpactKick` + a particle burst on each **landed** member and on the performer; `SensoryAudioAdapter` plays exactly **one** `CardVibeImpact` sting per card play (`FanoutIndex == 0`; immediate, never jittered). Gameplay only publishes — no direct `FxManager`/`AudioManager` calls from the effect path (Sensory Contract). **Why not `CardPlayedEvent`:** it fires *after* resolution, once per card, with no per-target delta — it cannot express a per-member block, and it is what the tutorial's beat-8 hold release keys on, so the impact publish being *upstream* of it is exactly what guarantees the FX is seen before the held loop is let go. **Authoring consequence (D-PW-AUDIO):** such a card is authored `AudioType = None` — the impact sting replaces the drop sting (`SSoT_Card_Authoring_Contracts.md` §5.15). Primary homes: `SSoT_Audio.md` §3 + inv. 18 (key + sink) · `Design_Sensory_Contract_v0_1.md` §3/§4 (event + FT presentation + audit) · `SSoT_Card_Authoring_Contracts.md` §5.15 (authoring rule). Smokes ST-PW-1..10 green, no deferrals. **Open:** placeholder clip (`Telephone`) → D1 (§4).

- **Unplayable-card overlay + single playability source — LIVE (CARD-UX-1, closed 2026-07-13).** An inserted demo-cut-close batch during S5h, not an S-sequence batch. **(1) One playability source of truth for display.** `GigManager.EvaluateCardPlayability(CardDefinition) → UnplayableReason` {`TutorialGate`, `ActionTiming`, `FinalLoopLock`, `Inspiration`, `Budget`, `None`} aggregates the gates the play paths already consult (never consuming: `CanConsumePlay` / `CanAffordInspiration`, never `TryConsume`); `HandController` polls it per-frame and drives `CardBase.SetUnplayableOverlay(bool)`, which reuses the existing `passiveImage` / `SetInactiveMaterialState` mechanism (red restyle) — **no new serialized field**, so the two-prefab wiring recurrence vector in §4 stays closed (D4=A). Invariant: no consumer computes playability locally. The **ECON-1 budget input is partially scoped** (D5): statically-resolvable payers only (`FixedPerformerType != None`); `AnyMusician` excluded pending **D-ECON-GENERIC** — enforcement (`TryConsumePlay`) unchanged. **(2) Final-loop composition lock (D2=A).** `CompositionSession.IsFinalLoopRunning` + a deny in `TryPlayCompositionCard` before any spend, mirrored for presentation-avoidance in `GigManager.TryPlayCompositionCard`. Rationale (code truth, previously undocumented): every composition drop during a running loop normalizes to `CurrentPart` and renders on that part's **next** loop, so on the final loop it never renders. **Exempt while `TutorialLoopHoldGate` is armed** (a held loop replays); `TutorialModalGate` is not exempt (modals suspend progression, they do not replay). With parts-per-song = 1, the demo's only final loop *is* the beat-8 held loop ⇒ the lock is structurally unreachable inside the tutorial; testing it needs a non-tutorial run (**DEMO-TUT-TOGGLE**, DEMO-FIXES). **(3) Spawn-hook tutorial highlights (D1=C).** `TutorialHighlightSpawnHook` + `TutorialHighlightTarget.InitRuntime` register highlight targets on runtime-instantiated characters / status icons / hand cards at `GigManager.BuildBand`/`BuildAudience`, `CharacterCanvas.TryCreateIcon`, and the `DeckManager.BuildAndGetCard` tails — closing the world-character + hand-card highlights deferred from TUT-R3. Duplicate keys disambiguated by re-registering the affected character on `MusicianStressHitEvent` / `AudienceBlockedEvent` (D3=B). T3b's world→screen edits are now applied in build. **(4) Beat-8 `SingleCardOnly` gate (added in-batch, D6=A).** Armed with the loop hold, hand-guarded; the finisher becomes the only draggable card, and this — not the lock — is what gates compositions in the tutorial's final loop. Blocks drag only, not End Turn. Primary homes: `SSoT_Card_System.md` §10.5 (playability display) · `SSoT_Runtime_CompositionSession_Integration.md` §5.4 + §8 inv. 11 (lock) · `planning/active/Design_Tutorial_System_v0_2.md` §4.2/§5.3 (gate + highlights) · `SSoT_Gig_Combat_Core.md` §14.5/§14.7 (overlay budget scoping). Smokes ST-R3b-2/5 + ST-CU-1..13 green. **Open:** R1 (beat-8 hold with the finisher in a pile — §4).

- **Multi-role tracks per musician — LIVE (BASS-1 + BASS-CARD-1, closed 2026-07-12).** An inserted cross-cutting fix during S5h, not an S-sequence batch. The composition model keys a part's tracks by **`(musicianId, role)`**; a musician may hold several role-tracks at once. Card semantics: same-role ⇒ replace, different-role ⇒ add. **A Track card with no `styleBundle` no longer creates a track** (D4=A) — it augments the matching-role track if one exists and otherwise applies only its part effect; PartEffect carriers (Key Lift, Push It, Half Time) rely on this, and it makes the bundle *mandatory* for any card meant to add a track. Stem cache + part-cache instrument pin were **disabled for parts containing a multi-track musician** (D3=A) because the package's stem/instrument readback was musician-keyed and could not represent two role-tracks; session pins (`musicianId|role`) carried voice consistency instead. **Superseded by DBG-C1 (2026-07-17):** the package re-keyed those surfaces `(musicianId, role)` and ALWTTT adopted the composite key end-to-end, retiring all three carve-outs — multi-track parts are cacheable again. Single-track parts are byte-identical throughout (ST-BASS-6, ST-S1). See the DBG-C1 bullet below / §1 table. Authoring: `trackAction.styleBundleCreate` mints a role-typed bundle from JSON. Primary home `SSoT_Runtime_CompositionSession_Integration.md` §11; schema `SSoT_Card_Authoring_Contracts.md` §5.13–§5.14; package constraint recorded in `SSoT_ALWTTT_MidiGenPlay_Boundary.md` §4.3. **Open item:** Sibi's two Bassline cards (Worm Walk / Worm Pulse) currently carry the `StarterDeck` flag as a testing convenience — starter status must be resolved before demo (`Design_Starter_Deck_v1.md` §5.19; recommendation: RewardPool).

- **Guided gig-1 tutorial — LIVE (TUT-REBUILD, closed 2026-07-10).** The first gig runs a driver-sequenced 10-beat guided curriculum (+ `tut_composure` + rewritten reactives) over the retained reactive system (D3=B). Runtime in `Assets/Scripts/Tutorial/` (driver, input gate, loop-hold gate, scripted draw queue, token resolver, highlight registry) + `Assets/Scripts/Sensory/` (`MusicianStressHitEvent`, `AudienceBlockedEvent` — the latter because Blocked is a sprite-tint bool, not an SO status (F2), so `StatusAppliedEvent` never fired for it). Input gates (beats 3/5) + holdLoop (beat 8) with mandatory degrade; **pacing holds gameplay progression under any modal while audio continues (D-TUT-R2b-1=B)** — this reverses the S4 "progression continues under modal" model for progression only; the audio no-freeze precedent is retained. Design authority: `planning/active/Design_Tutorial_System_v0_2.md`; arc home: `planning/active/TUT-REBUILD_Sub_Roadmap.md`. TUT-R2/R2b/R2c infra green (RT1–RT8). **TUT-R3 closed 2026-07-10:** doc pass (v0_2 + cross-doc riders + sub-roadmap) + the 9-superseded-id retirement applied in-build (parity guard green via the `ReservedUnauthored` exemption for the two reserved ids; `tut_first_reward_choice` authored ES+EN, closing a pre-existing S5h parity gap) + world→screen mask + per-beat highlight registration (static UI / hand / audience_area wired). All smokes green (ST-R3-1..7, ST-R3a-1..3, ST-R3b-1/3/4/6). Two starter cards authored: **Psychic Waves** (Sibi, Action, cost 3, `ModifyVibe +5` AoE — the guided finisher) + **Keep Cool** (**C2**, Action, cost 0, `composure +3` Self) — starter 15 → **17** (6 Action / 11 Composition). **Deferred out:** world-character + hand-card highlights → **delivered by CARD-UX-1 (2026-07-13)** via the spawn hook (see the CARD-UX-1 bullet above); Keep Cool's move to the generic catalog → **D-ECON-GENERIC**, still open (home `Design_Action_Economy_v1` / `SSoT_Gig_Combat_Core §14`).

- **Reward screen + venue-SFX unlock (#6b-lite) — LIVE (S5h, code closed 2026-07-07; doc pass 2026-07-13).** The final-encounter WinGig branch was de-bypassed (Win → RewardCanvas → WinPanel, D1=A); card-reward sourcing moved off the `RewardContainerData` asset list to the flag-driven `PersistentGameplayData.BuildRewardCardPool` (`RewardPool ∩ UnlockedByDefault` on the band catalogs, owned cards excluded, D2=B/D9); grants route through the provenance-aware `GrantRewardCard`, fixing `ChoiceCard`'s prior unconditional `CurrentActionCards.Add` that mis-filed composition rewards. **#6b-lite (user scope amendment 2026-07-07):** venue SFX are unlocked as gig rewards — sequential lights→smoke→fire (D6=A), run-scoped persistence surviving Retry but reset on fresh launch (D7=A, reset in `ApplyRunConfig`); a **locked** threshold is fully inert at its SongHype crossing (no VFX, no banked Vibe, no `SfxStageCrossedEvent`, D8=A — **S5i balance note:** gig 1 of a fresh run has no SFX Vibe layer); the SongHype bar activates once any threshold is unlocked (`AnySfxUnlocked` OR the S5f `ShowSongHypeBar` toggle, which stays OFF in the demo). New `RewardChoiceOpenedEvent`, `SensorySfxType.RewardOpened`, and a `tut_first_reward_choice` dialog (ES/EN). Presentation debt (no promoted presentation SSoT) is tracked in §4 per the S5b precedent. Full SFX-as-equipment (shops/events/slot assignment) stays Phase C. `ST-S5h-1..10` PASS; the non-final RewardCanvas branch is deferred to Dev Mode (unreachable in the demo). Semantic homes: `SSoT_Scoring_and_Meters.md` §6.2 (D8 gate) · `Design_Starter_Deck_v1.md` §Reward pool (D2=B) · `Design_Demo_Cut_v1.md` A6 row (routing).

- **Per-turn play economy — ACTIVE (ECON-1, closed 2026-07-07).** Each musician may play at most **1 Action + 1 Composition card per period** (period = pre-song PlayerTurn window, or each performance loop); pools independent. Budgets refill at three `GigManager` seams: `PlayerTurn` case (Seam A), `OnPlayPressed()` (Seam B — the song-start seam; the `SongPerformance` phase case is NOT live while `_session != null`), and `OnCompositionLoopFinished` (Seam C). Two pips per musician on `BandCharacterCanvas` (Action/Composition) show availability, pushed via `OnTurnPlayBudgetChanged`. Inspiration cost is an orthogonal gate, untouched. **D-ECON-6=DEFER (2026-07-07):** all starter cards set to cost 0; the "finisher" layer (cost > 0 cards) is designed but its card assignments are deferred to a future batch (finisher costs tuned in S5i). Primary home `SSoT_Gig_Combat_Core.md` §14; rationale `planning/active/Design_Action_Economy_v1.md`. **Debt:** the S4 tutorial copy has not been reviewed against the new economy (pips + 1+1 limit) — deferred, not rewritten.

- **Run telemetry logger — LIVE (TLM-1, closed 2026-07-16).** An `ALWTTT_DEV`-gated dev surface opened by BALANCE-XREF (BR-D2=A), slotted before S5i. `DevRunTelemetryLogger` (`Assets/Scripts/DevMode/`, whole file `#if ALWTTT_DEV`, lifecycle owned by `DevModeController` — sibling to `DevGigOutcomeTracker`) is a **read-only** sensory-bus subscriber that writes **one JSON-Lines record per gig** on `GigOutcomeEvent`: outcome + `lossCause`, songs/loops, per-audience end-Vibe snapshot (authored `CharacterName` + spawn index; taken before `WinGig`/`LoseGig` cleanup), ordered `plays[]` with **song-index-at-play-time** (the BALANCE-XREF "Madness"/SFX confound guard), per-card counts, roster + encounter label, `sessionId`/`timestamp`/`schemaVersion`. Output: Editor → `<projectRoot>/DevTelemetry/*.jsonl` (gitignored); dev players → `persistentDataPath`. The Stats tab shows a "last gig written to" line. Publishes nothing, mutates nothing, MidiGenPlay untouched; strips clean without `ALWTTT_DEV`. Decisions **D-TLM-1=A** (JSONL) · **D-TLM-2=B** (extended field set) · **D-TLM-3=A** (cohesion-collapse losses NOT logged — that path never publishes `GigOutcomeEvent`, same blind spot as the session tally; `lossCause` constant `"unconvinced_after_final_song"`; publisher-side fix deferred as **optional rider TLM-1b**). ST-TLM-1..4 + ST-TLM-R1 PASS. Primary home: `SSoT_Dev_Mode.md` §17 (+ §9.12 smoke rows). **This unblocks S5i** — pick-rate / dead-card / pacing observations are now recorded fact instead of n≈2 recollection.

- **Contextual stats on hover — feature disabled (2026-04-20).** `BandCharacterCanvas` hover-to-show-stats path present in code but disabled at prefab level (`statsCanvasGroup` / `statsRoot` unassigned, `StatsRoot` GameObject off). Silent no-op. Revisit when visual density is tuned.

---

## 3. Next active

**Demo cut close sequence (S1-S5) + Vertical slice (S6-S8 = Phase C).** 8 sessions total per the 2026-05-23 planning reframe (see §1 entry). Strict left-to-right sequencing; no two open in parallel.

> **Sequence status (2026-07-01):** S1, S2, S3a, S3-audio closed; the full audio work-stream (M-AUDIO-MIX, AUDIO-SFX-FIX, AUDIO-OST, AUDIO-AMBIENCE, AUDIO-CHAR-PROFILES 1/2) closed 2026-06-15/16; **TUT-JAM-SEQ** (the S4 jam-sequence design + doc) closed 2026-06-16; **S4** (tutorial implementation + guided jam) closed 2026-06-17 (smoke suite complete; D-S4-DEDUP double-show fix). **S5e closed 2026-07-02; S5f closed 2026-07-04; S5g closed 2026-07-06; ECON-1 (per-turn play economy) closed 2026-07-07; TUT-REBUILD (guided gig-1 tutorial — an inserted arc with its own sub-roadmap) closed 2026-07-10; BASS-1 + BASS-CARD-1 closed 2026-07-12; **CARD-UX-1 closed 2026-07-13** (unplayable-card overlay + single playability source + final-loop composition lock + spawn-hook highlights; doc pass applied 2026-07-13). **S5h closed** (code 2026-07-07; doc pass applied 2026-07-13 — reward screen + #6b-lite venue-SFX unlock). **JUICE-PW closed 2026-07-13** (Psychic Waves presentation — per-target `AudienceVibeImpactEvent`, `CardVibeImpact` sting, procedural impact kick; ST-PW-1..10 PASS). **DEMO-FIXES-A closed 2026-07-15** (DEMO-TUT-TOGGLE gig-open opt-in + R1 beat-8 `HandHas` + CT1 persistent pulse + DF-COST0 + DF-ECONTIP + DF-CATALOG; D-DF-1..8=A; ST-DF-1..6+8..13 PASS, ST-DF-7 deferred to Dev Mode). Remaining in DEMO-FIXES: **DF-INSPLOOP** and **DF-ARTIC** (backlog riders, may split out; definitions in the sub-roadmap). **BALANCE-XREF closed 2026-07-16** (docs-only balance-research consolidation) and **opened TLM-1** — an `ALWTTT_DEV`-gated run telemetry logger (BR-D2=A). **TLM-1 closed 2026-07-16** (`DevRunTelemetryLogger`; ST-TLM-1..4 + R1 PASS; D-TLM-1=A/2=B/3=A; surface in `SSoT_Dev_Mode.md` §17), so the live front is now **S5i**. The tester-driven REPLANNING session closed 2026-07-01, locking the pre-tuning sub-sequence S5e → S5f → S5g → ECON-1 → S5h → TLM-1 (closed) → **S5i** (win-rate/analysis; was S5c) → S5j (close; was S5d); see the sub-roadmap ledgers D-REPLAN-1..6 and BR-D1..4.** The audio batches were not part of the S1-S5 numbering; they slotted around S4. S5 closing still unblocks Phase C (S6-S8).

### Demo cut (S1-S5)

| Session | Scope | Owning doc(s) | Status |
| --- | --- | --- | --- |
| **S1** | B3-slate-F: `AudienceCharacterSimple.ResolveLoopEffect` real implementation + per-audience FT (visual-only; SFX deferred to S3 per D2 documented exception) | `Roadmap §5.3` addendum + `Design_Sensory_Contract §3` (S1 direct-call note) | **closed 2026-06-12** (ST-S1-1..8 PASS; see §1) |
| **S2** | Sensory Event Bus foundation: bus + 2 event types (`AudienceReactionEvent`, `SongEndVibeEvent`) + thin `SensoryFxAdapter` (VerifyOnly) + `GigManager` coexistence publish (direct calls retained). TutorialController deferred to S4 (D-S2-5); direct-call deletion + adapter Spawn flip deferred to S3 (D-S2-3=A coexistence). | `Design_Sensory_Contract §3` | **closed 2026-06-14** (ST-S2-1..7 + S2-9 PASS, S2-8 N/A; see §1) |
| **S3a** | Sensory polish (visual): §4 full audit; `SensoryFxAdapter` Spawn flip + direct-call deletion; FT polish; Kid "Tantrum" animator; smoke / fire VFX on stage 2 / 3 (asset #4). Audio split out per D-S3-1=B. | `Design_Sensory_Contract §4 + §5` | **closed 2026-06-14** (ST-S3-1..13 + S3-4 PASS; see §1) |
| **S3-audio** | Audio SFX layer: `AudioManager` flesh-out + `SoundBankSO` (central inventory + coverage) + `SensorySfxType` + `SensorySfxPresentation` + `SensoryAudioAdapter`; card-play + reaction + song-end + stage-crossing wired; `SfxStageCrossedEvent`. | `Design_Sensory_Contract §4 + §5A` | **closed 2026-06-14** (ST-SA-1..9 + A1..A4 PASS; placeholder clips; see §1) |
| **S4** | Tutorial implementation: bus-event extension (`CardPlayedEvent` + Inspiration-delta + per-loop-gain) + R1/R2 overlay primitives (U1/U2 resolved at open) + `TutorialController` + single-modal queue + 11 authored triggers incl. the 6-beat jam sequence; pause-menu revisit (already-fired-only) + reset | `Design_Tutorial_System §6A + §6 + §8` | **closed 2026-06-17** (ST-S4-1..11 + QUEUE/PERSIST/RESET/REVISIT/OPP/NODIR/GATE PASS; D-S4-DEDUP=B double-show fix; 9-negative code-verified per D1=A; see §1) |
| **S5** | Balance + validation + presentation + §5.4 closure. **Decomposed into S5a–S5j → `planning/active/S5_DemoCutClose_Sub_Roadmap.md`.** Post-replan (2026-07-01) sub-sequence: S5a/S5b (closed) → S5e (semantics) → S5f (Spanish onboarding) → S5g (music variety) → ECON-1 (play economy) → S5h (reward screen) → S5i (win-rate; was S5c) → S5j (close; was S5d). B3-slate remainder folded per D-REPLAN-4 (E-lite + G into S5e/S5f; C/D/#12–14 deferred). | `Roadmap §5.4 + §5.5` | **active — S5i** (S5a/S5b/S5e/S5f/S5g/ECON-1/TUT-REBUILD/BASS-1/CARD-UX-1/S5h/JUICE-PW/DEMO-FIXES-A all closed; BALANCE-XREF docs-consolidation closed 2026-07-16 and opened TLM-1; **TLM-1 closed 2026-07-16** — run telemetry logger, before S5i; S5c→S5i, S5d→S5j repositioned by the replanning session) |

### Vertical slice (S6-S8 = Phase C)

| Session | Scope | Owning doc(s) | Status |
| --- | --- | --- | --- |
| **S6** | Run structure: minimal ship hub scene; gig → reward → ship → next gig flow; pilot portrait integration; tutorial dialogues `tut_ship_hub_intro` + `tut_first_reward_choice` | `Design_Vertical_Slice §3.1` + `Roadmap §7.1` | queued |
| **S7** | Run content I: 2 new venues (asset images 7 + 10); 2 new encounters; 4 new audience archetypes; audience state machine (idle / hostile / vibing); tutorial dialogue `tut_audience_state_machine` | `Design_Vertical_Slice §3.2 + §6` + `Roadmap §7.2` | queued |
| **S8** | Run content II: boss (`AudienceCharacterBase` subclass per D-RUN-4=α); closing sequence; tutorial dialogues `tut_first_boss_encounter` + `tut_run_complete` | `Design_Vertical_Slice §3.3 + §7` + `Roadmap §7.3` | queued |

### Post-vertical-slice (deferred per D-RUN-6)

Ship interior batches (bar, chill, rehearsal), space map, ladder-mode formalization, meta-progression, full audio pass. Not slotted into the 8-session plan. Captured in `Roadmap §7.5` (out of scope for Phase C) and "Future milestones" section.

### Roster Expansion campaign (opened 2026-07-23 — R0, R1, R2 + R2c + R2d, R3 closed)

4-musician starter redesign + finisher-layer population + per-musician rewards + Track Card Levels, decomposed as **R0–R8** in `planning/active/RosterExpansion_Sub_Roadmap.md` (campaign decisions D1–D6 locked at planning). Phasing **D1=C**: R0 (design, no code) and R1–R3 (enablers outside the demo roster/deck) may interleave with S5i; R4+ open only after the S5j tag *(amended 2026-07-31, D-SEQ-3=A: R4+ opens after the **snapshot tag**, not the demo-cut tag)*. **Live front unchanged: S5i → S5j.**

- **R0 CLOSED 2026-07-23** (design-only). `planning/active/Design_Starter_Deck_v2.md` locks the 4-musician starter (22 cards / 18 unique); D-R0-1..12 locked.
- **R1 CLOSED 2026-07-23** (first campaign code in the build). Captivated amplification layer inside the existing `ApplyIncomingVibe` gate + `MeterTuningSO.captivatedVibeBonusPerStack` tuning surface + Captivated SO/icon in `StatusEffectCatalogue_Audience` + the **Wink** card + Cantante catalog cleanup. ST-R1-1..6 PASS incl. the demo-inertness regression. Authority: `SSoT_Status_Effects.md §5.8`. Decisions D-R1-1=A (helper-wide amplification) · D-R1-2=A (MeterTuningSO tuning home) · D-R1-3 (`IsBuff = false`) · D-R1-4=A (legacy Cantante entries kept, `flags = None`).
- **Demo build is unchanged by R1 and remains demonstrable.** Captivated has no sender inside the demo roster, and Wink is unreachable from both the starter-assembly and reward paths (both band-scoped). The interleaving-safety argument of D1=C is now empirically verified rather than only structural (ST-R1-6).
- R1 closed 2026-07-23; **R2 + R2c + R2d closed 2026-07-31**. R2c/R2d are the first campaign batches with code in the build (`MidiMusicManager` harmony wiring + `InstrumentEffect.RandomFromList`); both surfaces are BC-gated and the demo config leaves the default-harmony palette unassigned, so the S5i baseline is unperturbed by construction (ST-R2d-4 + ST-R2d-7 PASS). **R3 (Zig composition cards) is the last interleavable enabler.** Live front unchanged: **S5i → S5j**.

- **R3 CLOSED 2026-08-08.** Catálogo Cantante: Wink (R1) + **Rise Up** + **Showtime**, las tres StarterDeck, 1 copia. Fuera del roster de demo por construcción (band-scoped); demo-inertness re-verificada (ST-R3-12, y C5: gig completo sin Cantante y sin cartas Wormus ni en mazo ni en pool). **Rise Up** operativa: patrón autorado de 8 compases por grado, adaptativo a raíz (verificado en A#, B, E, G# Ionian) y a modo (verificado en Aeolian). **Showtime** operativa (ST-R3-11 PASS): ruta procedural con paleta de frases anémica. El lote entregó además **JAM-1** y **JAM-2** en `CompositionSession`, tres cartas Wormus de banco de dev (`flags=None`) y la poda de `Chord Palette - Modal` (7→5).

### Pending content queue (CONT-B, 2026-07-31 — registered, not executed)

1. **`Modal Drift` card for Sibi** — JSON payload delivered (`Card_Sibi_Modal_Backing.json`, ref `Backing Card Config - Modal`); must land in **`Sibi_CardCatalogData`**, not the generic catalog (D-INSP-6=A': the reward pool resolves per musician). `fixedMusician` / `rarity` unset in the JSON — resolve in the staged form against the real enums. Before re-importing any existing asset (esp. `Prog_Maj_Ragtime_SECDOM`, the only one with per-event marks that vanish without trace), read the CPE-META-1 D2 resolution in the MidiGenPlay authoring SSoT (CONT-B §9c.2).
2. **`FF_Metal8c`** — lighten the bar-8 crash to one hit per beat (F-METAL-1).
3. **`SP_DnB8c`** — re-author at `subdivisions = 8` and compare before touching anything else (F-DNB-1). Items 2–3 are now cheaper via the DPE-IMPORT-1 header payload (CONT-B §9c.3); check whether the reverse render (asset → payload) exists to capture and version the 8 current patterns as text.
4. **`FF_LatinSon32_8c`** — **RETIRE from its palette (do not delete)** until D10 resolves. F-KIT-1 flips from non-blocking to **BLOCKING** if the pattern stays demo-reachable: the palettes draw randomly and a player can receive a pattern with 3 of 4 lanes muted.

### Standing directives applicable across all sessions

- **D1** Sound Design Priority (`Design_Project_Directives §D1`, promoted 2026-05-20).
- **D2** Sensory Contract (`Design_Project_Directives §D2`, promoted 2026-05-23). *(line reconstructed from the reframe block — same batch, low risk)*
- **D3** Tutorial-as-mandatory (`Design_Project_Directives §D3`, promoted 2026-05-23). *(line reconstructed from the reframe block — same batch, low risk)*

## 4. Open items and risks

### Open items (non-blocking)
- **D9 — Fingered vs Slap level balance (CONT-B, 2026-07-31 — OPEN).** Fingered Bass sits below Slap Bass in the mix; the imbalance is **payload-born, not instrument-born** (Pocket boosts are slap/pop-exclusive). Options: (A) raise `volume01` on the shared `Fingered Bass` package asset; (B) ask MidiGenPlay for card-level gain/velocity on `BasslineCardConfigSO`; (C) lower the slap card's Pocket boosts. Recommendation on record: **A now + B filed as ask** (C rejected — the boosts are what makes slap read as slap; T1.3/T1.4 just validated them). Homes: `SSoT_Audio.md` §4.6 granularity note · CSV sub-roadmap §5 (CONT-B ask 3) · returns doc (pending).
- **D10 — pattern ↔ drum-kit compatibility (CONT-B, from F-KIT-1 — OPEN).** No mechanism guarantees a drum pattern is played by a kit that maps its instruments. Options: (A) restrict the authoring vocabulary to the all-kit intersection (cheap; impoverishes content, kills the latin pattern as authored); (B) pattern→kit compatibility table (whitelist or weighted list) consulted at selection — the correct fix, needs a new structure and a home decision; (C) package ask to widen kit mappings (claves/congas/cowbell). Recommendation on record: **C + B**; A only under demo pressure. **Interim: retire `FF_LatinSon32_8c` from its palette (do not delete).** F-KIT-1 becomes **blocking** while that pattern remains demo-reachable.
- **[RESUELTO 2026-08-03 — CTX-2a] D11 — default part tempo (CONT-B, from F-TEMPO-1).** Resuelta **=A**: el default del modelo pasa a `Slow`. Historia completa en el changelog; regla viva en `SSoT_Runtime_CompositionSession_Integration.md` §12.4. Queda abierta solo la calibración de la banda (**D11b**, abajo).
- **D-R4-1 — ¿el público juzga la tonalidad autorada o la que suena? RESUELTA 2026-08-08 = RECONOCER, NO CONSUMIR.** El público **sí debe** juzgar lo que suena; **no se cablea en este demo**. `LoopFeedbackContext` se construye desde el modelo de UI, así que bajo armonía modal la audiencia evalúa Ionian aunque JAM-2 haya propagado Dorian al render. Eso pasa de *hallazgo sin dueño* a **deuda conocida con dirección decidida**: queda prohibido "resolverlo" hacia la tonalidad autorada, que es la dirección hacia la que el código se inclina hoy por inercia. Homes: `SSoT_Runtime_CompositionSession_Integration.md` §13 (límite del seam) · `SSoT_Audience_and_Reactions.md` (preferencias) · sub-roadmap §8.
- **D-R4-2 — origen del calificador de canción (HAPPY / SAD / FUNKY). RESUELTA 2026-08-08 = A, con B como override opcional.** El calificador se **deriva**, no se autora: todos los modos se agrupan en **Major / Minor** por su tercera (Ionian/Lydian/Mixolydian → Major; Aeolian/Dorian/Phrygian/Locrian → Minor) y el bucket sale del modo **sonante**, dato que JAM-2 ya calcula. Cero autoría nueva. **Razón de rechazar el calificador autorado como fuente primaria:** se desincroniza de lo que suena, que es el bug de D-R4-1 replicado en otro campo y esta vez **sin forma de detectarlo**, porque no habría nada contra qué contrastar. **Dirección (no comprometida):** el eje compuesto —armónico (Major/Minor) × rítmico (síncopa / pocket de la pista de Rhythm)— porque «FUNKY» no es un modo y un eje puramente armónico nunca lo distinguirá de un pop mayor. **OPCIONAL, no bloqueante:** mostrar el calificador en la descripción de la carta con color/tipografía destacada. No es requisito de R4; entra si Read the Room necesita revelar preferencias de calificador.
- **D12 — merge Sibi backing + bassline (CONT-B — OPEN, deferred by user decision).** Registered with its trade-off in `planning/active/Design_Action_Economy_v1.md` §7. Status quo (A) kept this session.
- **D11b — cuánto más rápido debe ser `Slow` (CTX-2a, 2026-08-03 — ABIERTA, medición DIFERIDA a post-R3).** D11=A puso el default en `Slow`; queda por calibrar la banda. **La decisión no se toma sin dato**, y el dato es doble: (i) el BPM legible por oído para percusión de 8 compases *y* para melodía cantada (Zig, voz articulatoria); (ii) la **dispersión** del BPM resuelto entre partes marcadas `Slow` a lo largo de una canción completa, no de una sola parte. Motivo de (ii): el BPM se resuelve por parte y se cachea (`SSoT_Runtime_CompositionSession_Integration.md` §8 inv 13); si la dispersión domina, D11b cambia de *desplazar la banda* a *estrecharla*. El instrumento de medida ya existe (Dev Mode §18.13 + el log `[BPM] Resolve part …`). **Diferida por decisión de usuario hasta después de R3**, que es también cuando habrá melodía cantada real que auditar. Ejecución posterior: lote corto en el proyecto **MidiGenPlay** (las bandas del enum `TempoRange` son package-side), no un ask externo — el paquete es propio.
- **CTX-2b — override de articulación [RESUELTO 2026-08-03].** Fila 14 respondida
  (determinista, §8.1/§8.3/§8.5 del composer). D-CTX2B-1=A clon en runtime ·
  D-CTX2B-2=A sin ask nuevo. Implementado; ver `SSoT_Dev_Mode §18.14`.
- **F-KIT-1 — ningún kit de percusión mapea claves, congas ni cencerro (CONT-B, T4.5 FAIL).** El patrón `FF_LatinSon32_8c` usa `Claves` (75), `OpenHiConga` (63) y `Cowbell` (56); `RhythmTrackComposer` los reporta como no reproducibles y **mutea 3 de sus 4 lanes** con Jazz Kit — comportamiento correcto del paquete (warn + mute, no crash), el hueco es de contenido. Los 9 kits reportan `percussionMappings: 8`, así que la carencia es uniforme, no del kit elegido. Los sustitutos de familia tampoco existen (`Cowbell` no tiene ninguno declarado). **Consecuencias:** (a) el patrón latin suena reducido a bombo; (b) no hay hoy forma de garantizar que un patrón se toque con un kit compatible — no existe tabla patrón↔kit. Los kits son assets del paquete → ask package-side (ver §7). Decisión ALWTTT owed: **D10** (§6.3).
- **F-VEL-1 — los ghost notes suenan demasiado bajos (CONT-B, T4.2/T4.3/T4.8).** El glifo `o` mapea a un tier fijo de velocity (50) frente al golpe normal; observado en funk, jazz y country: los ghosts casi no se oyen, y el contraste con los finales acentuados es excesivo. Los tiers son constantes del paquete (`DrumPatternTextParser`), no parametrizables desde el asset. Ask package-side (§7). Mitigación ALWTTT disponible mientras tanto: subir el `defaultVelocity` de la lane afectada, que desplaza los tres tiers a la vez (paliativo, no arreglo).
- **[RESUELTO 2026-08-03 — CTX-2a] F-TEMPO-1 — el tempo por defecto de la parte hace ilegibles los patrones (CONT-B).** Cerrado por el mismo cambio que D11=A. El default del modelo es ahora `Slow`; la escucha de percusión de CONT-B ya no necesita override manual.
- **F-DNB-1 — `SP_DnB8c` con golpes fuera de tiempo (CONT-B, T4.7 parcial).** Algunos onsets no se perciben on-grid. Hipótesis **no verificada**: la granularidad `subdivisions = 4` (semicorcheas) no basta para expresar la colocación del two-step de drum'n'bass, que en el género real vive en fusas o con desplazamiento. Verificar re-autorando a `subdivisions = 8` y comparando antes de tocar nada más.
- **F-METAL-1 — el crash final de `FF_Metal8c` es demasiado denso (CONT-B, T4.6).** El compás 8 lleva crash en semicorcheas; el soundfont no lo resuelve limpiamente y suena embarrado. Arreglo de contenido, barato: un crash por beat en lugar de 16avos. No requiere decisión.
- **F-QUAL-1 — brecha de calidad entre patrones nuevos y antiguos (CONT-B, observación).** Los 8 patrones nuevos suenan notablemente mejor que los preexistentes de las mismas paletas (FF_*, WL_*, OM_*, CS_*), que ahora conviven con ellos en el sorteo. No es defecto: es deuda de contenido heredada, y las paletas mixtas exponen la comparación. Candidato a pasada de re-autorado con el mismo estándar de 8 compases.

> **Nota de bloqueo (CONT-B):** F-KIT-1 pasa de *non-blocking* a **bloqueante** si `FF_LatinSon32_8c` queda alcanzable en el demo, porque SyncopatedPocket/FourOnTheFloor sortean y el jugador puede recibirlo. Mientras D10 no se resuelva, la salida más barata es retirar el patrón latin de la paleta (no borrarlo).
- **Dormant cache duty — `SlapPocket` (2026-07-31).** If any ALWTTT bundle sets `BasslineCardConfigSO.pocketMode = SlapPocket`, the Bassline `trackInputsHash` must be extended with the resolved Rhythm pattern inputs in the same batch, or the cache can replay a bass stem rendered against a replaced drum pattern. **Not armed:** ALWTTT's slap content uses `SelfPocket`, which reads no other track. Re-arms for `SlapPocket` or a future `Auto` mode. Home: `SSoT_ALWTTT_MidiGenPlay_Boundary.md` §8.4.
- **Default-harmony palette token does not track palette contents (2026-07-30, accepted).** The `dp:` cache segment identifies (palette asset, seed, part), not its entries or weights, so editing the palette mid-session does not invalidate. Harmless while caches are per-song (D7=B). Home: `SSoT_Runtime_CompositionSession_Integration.md` §8 inv 9.
- **Two package-declared gaps at the harmony boundary (2026-07-31, out of scope).** (a) The bass's private `Pattern` receives no meter normalization or re-qualification — private harmony, outside the shared channel. (b) A palette that passes the package's harmony sniff but whose pick fails on time signature at compose time makes Backing degrade to procedural **without the suppressed host default resurging**. Not silence; pre-existing "failed palette pick" semantics. Home: boundary §8.6.
- **`Auto` pocket mode — registered, deliberately not requested (2026-07-31).** "SelfPocket without drums, couple to Rhythm when present" is desirable but re-arms the §8.4 cache duty (conditional cross-track dependency ⇒ needs an `rh:` hash segment) and would change the bass's figure mid-song when a drum card lands. Revisit with SLAPFIG-2 once real drum content exists.
- **BALANCE-XREF forward-pointers — Phase C / meta-progression (opened 2026-07-16).** The deckbuilder balance-research cross-reference (docs-only; maxims doc `planning/Design_Game_And_Card_Maxims_v0_1.md`; changelog 2026-07-16) folded its *cheap now-relevant* pieces into the sequence (TLM-1 logger — **delivered 2026-07-16**; S5i lenses L1–L3) and **forward-pointed** its expensive structural items, which have no batch yet:
  - **Run power curve (Phase C S6/S7).** A run must feel like a rising arc (maxim N8); candidate spine = full SFX-as-equipment (#6b) + reward-pool growth beyond the current ~2 cards. Scope decision + acceptance target recorded in `Design_Vertical_Slice_v0_1.md` §3.1 / §11.
  - **A "break-the-game" combo (Phase C / roster expansion).** The genre's poggable-moment appeal (maxim N4); the natural first candidate is the deferred **Earworm × Captivated** pairing (Ziggy) — design it *as* the aspirational, hard-to-assemble combo, not just a status pairing. Ties to `planning/Design_Audience_Status_v1.md`.
  - **Difficulty ladder (meta-progression).** A Covenant/Ascension-style ladder doubles as telemetry segmentation (maxim N11); plan it early in meta-progression design, the day the tester pool exceeds ~5.
  These are pointers, not commitments; each attaches to the batch that owns it. Not blocking the demo cut.
- **D-CSV-16 — no card → bundle reverse index (escalated to blocking, CSV-4, 2026-07-20).** `CompositionInventoryWindow` indexes references downward (bundle → palette → pattern) but not `CardDefinition → TrackStyleBundleSO`, so it cannot say whether a bundle is reachable from any card. Consequence made concrete at CSV-4: the reachable-progression figure (14 of 33) and the liveness of the Modal and Test palettes were established **by recollection, not by tooling**. Decision locked as **A** (the index is owed) but **no batch owns it**. Homes: `SSoT_Editor_Authoring_Tools.md` §17.10 · sub-roadmap §3.
- **D-MEL-1 — RESOLVED =A (CSV-3, 2026-07-22).** A part's meter is owned by a `MeterEffect`: it is a model-construction `FourFour` default mutated only by a `MeterEffect`, so a non-4/4 rhythm card must carry a matching `MeterEffect` (Pentameter precedent). The `?? default` expression cited at open is the audience/`LoopFeedbackContext` path, not the render path (location correction). This unblocked the CSV-4 listening pass. Homes: `SSoT_Runtime_CompositionSession_Integration.md` §12 · `SSoT_Card_Authoring_Contracts.md` §5.16.
- **D-BAG-3 / MGP-MIX-1 — package-side dependency, no consumer-side per-instrument gain (registered CSV-4, 2026-07-20).** `volume01` is `1.0` on all 70 melodic instruments — unauthored, not deliberately flat — and it is a package-authoring field ALWTTT must not edit (D-CSV-7=A). A package seam exists (`MidiGenerator.ApplyChannelVolume` CC7 + `IMixController`); what is missing is a documented per-musician gain that composes multiplicatively with `volume01`. ALWTTT input was delivered 2026-07-20. **Until it closes, no ALWTTT mix-balance batch can run.** Home: `SSoT_ALWTTT_MidiGenPlay_Boundary.md` §4.3.
- **R1 — the beat-8 tutorial hold can arm with the finisher in a pile (opened CARD-UX-1, 2026-07-13).** `TutorialLoopHoldGate`'s `available` check accepts `HandHas || PilesHave`, and held loops grant no draw (no `LoopResolvedEvent`), so if the beat-7 scripted draw fails the loop can hold with the finisher unreachable. **Pre-existing since TUT-R2**; the new `SingleCardOnly` gate does not worsen it (it is hand-guarded and simply does not arm). One-line hardening specified (`available &&= deck.HandHas(IsFinisher)`). Triaged to **DEMO-FIXES**. Home: `Design_Tutorial_System_v0_2.md` §9.2.
- **D-ECON-GENERIC — open (who pays for an `AnyMusician` card).** Attribution today is fixed performer → hover → `SelectedMusician` (D-ECON-3=A). Two things now wait on it: Keep Cool's move to the generic catalog, and the **`AnyMusician` half of the unplayable-card overlay's budget input** — CARD-UX-1 shipped that input **partially scoped** to statically-resolvable payers (D5), because a false red on a card that *is* playable against another musician is worse than a false green on an advisory overlay. ECON-1's rule and its enforcement are unchanged. Homes: `planning/active/Design_Action_Economy_v1.md` §7 / `SSoT_Gig_Combat_Core.md` §14.5.
- **D-SENSORY-HOME — open (authority home for the sensory carril; surfaced JUICE-PW-DOC, 2026-07-13).** The bus event inventory + FT presentation live in `Design_Sensory_Contract_v0_1.md`, classified **reference/planning — not a SSoT**, while `SSoT_Audio.md` explicitly disclaims owning the bus. The carril has grown a real contract (11 shipped events, 2 SFX keys, 3 adapters, an authoring rule) with no SSoT home. Options: (A) promote a `SSoT_Sensory_Layer.md`; (B) fold bus + FT presentation into `SSoT_Audience_and_Reactions.md`; (C) accept the planning doc as de-facto authority. Recommendation on record: **A**, in its own batch — not as a doc-pass side effect. Until resolved, edits keep landing in the planning doc.
- **Sibi Bassline cards carry the `StarterDeck` flag as a test convenience (BASS-CARD-1, 2026-07-12).** Worm Walk + Worm Pulse were flagged starter so they could be drawn during ST-BASS-1..8. This puts the starter at 19 cards and contradicts `Design_Starter_Deck_v1.md` §4 (17). Resolve before demo: drop the flag, move to `RewardPool` (recommended), or promote bass to real starter content and re-derive the §4 counts. Owner: S5h or S5i.
- **LoopScore complexity term inert (S5e, owned by S5i).** The `0.5 × TotalComplexity` term in `LoopScoreCalculator.ComputeLoopScore` contributes 0 for all current content because `inspirationGenerated` was stripped to 0 project-wide (D-S5e-1=A, locked). Retained in code; S5i owns its replacement or removal when the inspiration economy is re-tuned. Semantic home: `SSoT_Scoring_and_Meters.md` §3.2. **DF-INSPLOOP rider (2026-07-16):** DF-INSPLOOP reintroduced per-loop Inspiration generation *without* touching this term (derived off `sourceCardDefinition`, never into `LoopTrackSnapshot`). Term stays inert. S5i now owns **two** items here: the complexity-term decision AND cleanup of the write-only dead-state `CompositionSession._buildingPartInspirationPerLoop`.
- **Basal per-loop Inspiration grant is 1, not 3 (doc↔asset divergence, surfaced DF-INSPLOOP 2026-07-16).** `GigFlowSettings.asset` `defaultInspirationPerLoop = 1`, so the runtime per-loop basal grant is **1**. The S5e closed-batch row states "fixed 3/loop" (the S5e-era code default / intent). The asset was tuned to 1 at some untracked point. Not a code bug — a documented-truth vs asset-truth lag. Reconcile in S5i when the inspiration economy is tuned; do not rewrite the historical S5e row. Home: `SSoT_Scoring_and_Meters.md` §3.2.
- **`starter_default_mode` is now cost=0/gen=0 (S5e watch item).** Under the fixed-income inspiration economy this starter card is economically free (no cost, no generation). Flag for the S5i tuning pass. DF-INSPLOOP does not change this card; the new per-loop generation path is card-specific (In the Pocket / Vamp), not a starter-wide change. S5i still tunes `starter_default_mode`.
- **[D-S5e-DOC-D — RESOLVED 2026-07-31, DOC-APPLY-1] Cross-SSoT convince-condition alignment.** The S5e inversion (`Vibe ≤ 0` = Convinced; `VibeGoal` retired into `MaxVibe`) is now reflected everywhere: `SSoT_Gig_Combat_Core.md` (§4.4 table + §5.3 rule) and `SSoT_Gig_Encounter.md` (§5.2/§8.1/§9.2) corrected in place; `Design_Starter_Deck_v1.md` and `ALWTTT_Combat_MVP_Audit_Final.md` carry dated correction banners (historical texts not rewritten); the three stale `VibeGoal` cached invariants in `ssot_manifest.yaml` were swept. One concept, one authority restored; code was already inverted.
- **Presentation / animation has no SSoT — `CURRENT_STATE` is the interim operational home (S5b debt).** The play-animation gating shipped in S5b lives only in code + here. `GigManager.ApplyBpmToStage` (runs per loop via `OnPartBpmResolved`) reads `CompositionSession.GetMusicianIdsWithTrackInPart(partIndex)` — a read-through of the `SongCompositionUI` model (that model is the authority) — and enables the beat/"playing" animation only for musicians who own a track in the played part; the active set is recomputed every loop. Non-performers are stopped via the new `CharacterAnimator` master gate (`SetBeatAnimationEnabled(false)` + `ResetToRestPose()`, authored per-toggle style preserved). `ResetStageToIdle` (between songs, via `OnCompositionSongFinished`) re-enables the whole band's idle sway. `CharacterAnimator` still carries a "move animation to its own class" TODO; when presentation/animation work grows, promote this to a dedicated SSoT and relocate this note. Until then, this section is the home for the gating + master-gate semantics. **S5h rider (2026-07-07, recorded 2026-07-13):** the reward screen and the venue-SFX unlock also have no promoted presentation SSoT — same interim home (this file), same S5b precedent.
- **Vibe transparency surfaces (S5a) — presentation home is the planning design note.** The C1 readout (`GigCanvas`) + C2/C3 per-enemy telegraph (`AudienceCharacterCanvas` + `VibeEffectiveness`) shipped in S5a; their presentation authority is `planning/Design_Vibe_Telegraph_v0_1.md` (now indexed + in coverage-matrix as "active (shipped S5a)", mirroring the Tutorial precedent), while the underlying math stays in `SSoT_Scoring_and_Meters.md` §6/§8. No dedicated systems-SSoT; if presentation work grows, fold this and the animation note above into one. The SFX song-end delivery (`_pendingSfxVibe`) is semantic and lives in Scoring §6.2/§8.
- **S5c (win-rate tuning) deferred → repositioned to S5i — replanning session CLOSED 2026-07-01.** First tester results prioritized core gameplay/UX/legibility over balance. The replanning session clustered 11 findings and locked a pre-tuning sub-sequence: **S5e** (meter inversion + inspiration simplification) → **S5f** (Spanish onboarding + first-gig shape) → **S5g** (≥5 musical patterns per composition card) → **S5h** (reward screen) → **S5i** (win-rate; was S5c) → **S5j** (§5.4 + close; was S5d). Ledger **D-REPLAN-1..6**; batch definitions in `planning/active/S5_DemoCutClose_Sub_Roadmap.md`. Win-rate tuning was re-prioritized behind the four new UX/content batches, not cancelled. The **[S5a-SMOKE] hype-24–53% observation** stays an S5i input, but now applies to gig-2+ only (S5f hides the SongHype bar in gig 1 by design). Deferred out of the demo cut: SFX-as-reward / cross-gig SFX unlock (needs multi-gig → Phase C S6); 3rd enemy + 2nd venue (fast-follow, Phase C S7); design-idea backlog (per-character action+composition split, mind-read card, breakdown rebellion).
- **Placeholder SFX → final authoring (D1).** S3-audio shipped with placeholder clips. Final, intentional, non-generic SFX per `AudioActionType` + `SensorySfxType` is a tracked follow-up (sound exists, so §7 is satisfied; the quality upgrade is documented, not deferred-as-silent). Per-character profiles (AUDIO-CHAR-PROFILES) are the structured surface for this. **JUICE-PW rider (2026-07-13):** `CardVibeImpact` shipped on the placeholder clip `Telephone`. It is the sting on the tutorial's beat-8 finisher — the single most-heard SFX in a demo showing — so it is the highest-value item in this backlog.
- **Music mix / "sometimes too loud" — DONE (M-AUDIO-MIX, closed 2026-06-15).** Dev "Audio Mix" tab + persisted per-musician/global/master-SFX balance shipped; `SSoT_Audio.md` created (D-AUDIO-SSOT=B). Remaining audio work (OST, ambience, per-character profiles) is sequenced in `planning/active/Roadmap_Audio.md`. Boundary held: `MIDIInstrumentSO.volume01` not wired.
- **Sensory Contract §4 full audit descoped from S2 → S3 (2026-06-14).** S2 was deliberately narrowed (bus + 2 event types + 2 publish sites + 1 subscriber) and did not walk the whole codebase to fill the §4 sensory-coverage audit, despite the original §3/§4 wording implying it would. The full audit lands in S3 alongside SFX coverage. Non-blocking; only the two S2 rows carry an as-built emission-path update today.
- **Shaken restrictions:** status applies and expires correctly; no gameplay gate yet. Design decision still open.
- **Audience Feedback DoT:** no Stress path on `AudienceCharacterBase`. Deferred.
- **Composure penalty during Shaken:** design intent only; not code-enforced.
- **True card copies in decks:** RESOLVED by M4.4 (closed 2026-04-29). `BandDeckData` is now a multiset; `PersistentGameplayData.SetBandDeck` expands counts into independent references; pile lifecycle preserves identity per reference. See `SSoT_Card_System.md §13` and `SSoT_Card_Authoring_Contracts.md §5.10`.
- **M4.5 architectural decision (filtered-draw mechanism):** RESOLVED 2026-04-30. Option 1 (predicate-based filtered draw on `DeckManager`) + subtractive budget rule. Two-hook framing collapses to single PlayerTurn-entry site because action and composition windows open simultaneously. Composition wins tie-break. See `SSoT_Runtime_Flow.md §4.2` and §1 M4.5 closure block.
- **M1.2 multi-turn validation gap** — fully closed (T5/T8 Phase 2, T7 Phase 3.1). No remaining deferred tests.
- **Choke-on-stunned design decision:** T5 surfaced that `HandController.TryResolveCardTarget` refuses stunned musicians. MVP decision: keep the refusal.
- **`CardActionTiming` default excludes PlayerTurn:** documented in `SSoT_Dev_Mode.md` §8.4 and `SSoT_Card_Authoring_Contracts.md` §3.4.
- **C1 — resolved (2026-04-26).** `AddStressAction` now routes through `ApplyIncomingStressWithComposure`. See §1 M4.1 closure block.
- **`CompositionCardPayload.effects` support — verified (2026-04-23):** ST-M13c-6 confirmed that `CardPayload.Effects` on composition cards works end-to-end (status tooltip appears on hover, effect authored via Card Editor). `Four on the Floor`'s `ApplyStatusEffect(flow)` co-effect is viable.
- **Raw Inspector `[SerializeReference]` drawer for `CardEffectSpec`:** Unity's default property drawer doesn't show a type menu for new list elements. Card Editor window is the intended authoring path. Defer custom drawer to M1.1.
- **Composition card face does not surface `CardPayload.Effects`:** by design (2026-04-21 simplification). Tooltip covers discoverability. Design question for M4 when composition cards with effects ship in player content.
- **Runtime tuning values pending from user:** `maxVibeFromSongHype`, `MaxCardsOnHand`, draw-per-turn. Required for calibrating `MaxVibe` of Heckler/Critic encounter archetypes (`VibeGoal` retired in S5e). Flow tuning values now landed (`flowActionVibeBonusPerStack = 1`, `flowVibeMultiplier = 0.08f`, Inspector-tuneable). Does not block M4.3; does block the starter v1 authoring tuning pass in M4.6.
- **Keyword-driven runtime behavior (surfaced M1.3b, 2026-04-23):** `ExhaustAfterPlay` bool and `Exhaust` keyword are currently independent. Planned resolution: retire per-keyword bools in favor of `Keywords.Contains(...)` checks, making the keywords list the single source of both tooltip and runtime behavior. Touches the card-play pipeline. Not yet scheduled.
- **Inspiration dual-siting (surfaced M1.5 P3.2, 2026-04-23 — substantially closed by 2026-05-08):** `pd.CurrentInspiration` and `CompositionSession._currentInspiration` are mirrored via `CompositionSession.AddCurrentInspiration` on all canonical paths. F-3 closed comp-card per-loop gain. MB3 closed the Dev path (`LiveInspiration` / `DevSetInspiration` / `DevSetCurrentInspiration`). MB4 closed the action-card path (`GigManager.AdjustInspiration` wrapper + `CardBase.SpendInspiration` / `GenerateInspiration` rerouting). One un-mirrored write remains: `TryPlayCompositionCard` step 8 (comp-card spend during build phase) — intentionally preserved, deferred to loop-game-flow milestone. The MB4-diag `[PD/Session]` Stats-tab readout makes any divergence directly visible. Potential follow-up: one-line note in `SSoT_Gig_Combat_Core.md` §4.2 to surface this implementation reality. See `SSoT_Dev_Mode.md` §13.4.
- **Musician picker in Gig Setup — RESOLVED (2026-05-04, M4.6-prep merged (1)/(4)).** Bidirectional band picker shipped. `pd.MusicianList` is now mutated by the picker before auto-assembly runs; `pd.SetBandRoster(picked)` handles roster identity. Validation: min 1 (warns), max 4 (blocks). ST-M42-6/7/8/9 are unblocked but not yet executed; they may run in parallel with M4.6 demo prep or post-demo. See §1 closure block.
- **Per-musician starter decks — RESOLVED (2026-05-02, M4.6-prep batch (2)).** `PersistentGameplayData.SetBandDeckFromMusicians` materializes the deck from each musician's `CardCatalog` (starter-flagged entries × `starterCopies`) plus an optional `GenericCardCatalogSO` from `GigSetupConfig.GenericStarterCatalog`. Toggle in Gig Setup scene (`useMusicianStartersToggle`, default ON) selects between auto-assembly and the legacy `BandDeckData` dropdown path. Provenance: per-musician contributions tracked, generic contributions not tracked. See §1 batch (2) closure block.
- **Gig Setup roster pickers — RESOLVED (2026-05-04, M4.6-prep merged (1)/(4)).** Audience picker shipped alongside the band picker. `GigEncounterSO.audienceMemberList` is now the *default* per-encounter audience composition; `GigSetupController` produces an `audienceOverride` when picker selection differs from baked, passes it to `GigEncounterSO.BuildRuntime(audienceOverride)`. Comparator is multiset-blind on baked duplicates (encounters with `[A, A, B]` preserve duplicates at runtime when user does not customize). Encounter-swap rebuilds picker with new defaults and warns if customization is discarded. See §1 closure block. Picker UI multiplicity (per-row count input) is a future concern tracked as M4.6-prep batch (6).
- **Card Editor inline effects-block UI on legacy catalogue alias — RESOLVED (2026-05-06, M4.6-prep cleanup, Patch 1).** `DrawStatusEffectPicker` now consumes `ALWTTTProjectRegistriesSO` and reads from both `StatusCatalogueMusicians` and `StatusCatalogueAudience`. UI is `DropdownButton + GenericMenu` with `Musicians/...` and `Audience/...` hierarchical paths. See §1 cleanup closure block.
- **All-starter-flagged catalog content (M4.6 demo blocker) — RESOLVED for demo roster (2026-05-06, M4.6-prep cleanup).** Robot and Gusano catalogs cleaned and authored to spec (Robot 4/4/5, Gusano 4/4/4; Generic 2/2/3 added). Cantante (7/7) and Conito (10/10) intentionally untouched and inert — they are not in the demo roster (M4 reduced to Robot C2 + Gusano Sibi). If post-demo roster expansion brings Cantante or Conito into play, their catalogs will need analogous cleanup. See §1 cleanup closure block. Verification snapshot: `inv4.json`.
- **M4.6F-1 Action card double-discard — RESOLVED (2026-05-07).** Bug was misdiagnosed at intake as a reshuffle/pile lifecycle defect. Root cause was upstream: `HandController.PlayCard:580-581` and `CardBase.Use`/`CardUseRoutine` both called `DeckManager.OnCardPlayed` for action cards, doubling the discard (HandPile.Remove + DiscardPile.Add fired twice per play, removing two distinct entries from HandPile because pile multiplicity tracks references). Composition cards were unaffected (they bypass `CardBase.Use`). Fix: gate the `HandController.PlayCard` call to `IsComposition` only. Latent SFX action card double-discard fixed by the same gate. Suspicion S-A (missing `SetPileTexts` at reshuffle) not the cause; suspicion S-B (duplicate `DeckManager`) ruled out. Smoke ST-DOUBLE-1/2/3 + ST-RESHUFFLE-1/2/3 all PASS. New invariant in `SSoT_Card_System.md §9.3` + `ssot_manifest.yaml`. See §1 closure block.
- **M4.6F-2 GigSettings unification — multi-SO refactor — RESOLVED (2026-05-07).** Settings dispersed across five homes consolidated to four SOs on the GigManager side (`GigFlowSettingsSO`, `MeterTuningSO`, `GigPresentationSO`, `GigDevSettingsSO`) plus renamed `GigSetupRosterSO` on the Gig Setup side. `GameplayData↔PersistentGameplayData` duplication remains by design (D4 deferral). Façade properties preserved on `GigManager`. Scene refs inline. Smoke ST-F2-S1..S8 ran with expected per-loop FAIL (S5 → F-3) and a pre-F-2 floating-text visibility caveat on S4. See §1 closure block.
- **M4.6F-3 Per-loop draw + per-loop inspiration hook + canonical AddCurrentInspiration — RESOLVED (2026-05-08).** New `GigFlowSettingsSO.DrawPerLoop` field. Per-loop draw + per-loop inspiration consumption fire from `GigManager.OnCompositionLoopFinished` (host-owned subscriber to `CompositionSession.LoopFinished`, respects deck-non-mutation invariant). `CompositionSession.AddCurrentInspiration(int) → int` promoted to canonical session-budget mutator (clamps to MaxInspiration, mirrors to PD, returns actual delta). Track-derived per-loop gain refactored through it. `+N` badge displays un-clamped track contribution. `JamRules.drawPerPart` flagged UNUSED (F-5 cleanup). Smoke ST-F3-S1..S7 + S4b PASS, S4c FAIL DEFERRED → MB3. Side-findings opened: Dev surface drift (MB3), session-start dual-siting (MB3), F-2 D4 follow-up (post-demo). See §1 closure block.
- **MB3 — Dev surface drift correction + session-start dual-siting fix** RESOLVED 2026-05-08. Code +25 / docs in §13.4 / §9.10. ST-MB3-1/2/4/8 PASS; ST-MB3-3 INVALID; ST-MB3-5/6/7 deferred to loop-game-flow.
- **MB4 — Action-card inspiration session routing (+ MB4-diag readout)** RESOLVED 2026-05-08. Code +37 / −2; +21 lines diag observability. ST-MB4-1..5 PASS. Closes user-reported critical action-card bug. F-followup queue exhausted post-MB4.
- **M4.6F-5 Composition next-loop pending workflow — ABSORBED into Phase B B1 (2026-05-09).** Original framing assumed per-loop pending was new functionality; user clarified during Phase B planning that per-loop card resolution **already works** in the current zone (cards in current → replace track → effect at next loop). The complex piece — *next zone* (planning a future part) — is not closed but **simplified out**: B1 disables next zone, current zone becomes full-screen, model collapses to per-loop-only. F-5 retroactively re-scoped; closure happens when B1 lands. See §1 Phase A close block and `Roadmap_ALWTTT.md §5`.
- **Phase B B1 — Loop model simplification + track persistence + UI rework — RESOLVED 2026-05-12.** See §1 closure block. All internal items (#7 stem cache, #0 next-zone disable, #1+#2 UI rework, #8 hand-discard configurability, #7.1 instrument pin, D-J draw-on-play mini-item) shipped. Smoke tests ST-B1-S1..S10 PASS or DEFERRED with reason. F-1, F-3, F-4 Stage A invariants clean. ~600-700 LoC ALWTTT-side across 9 files. The spike (D5) estimate of 300-400 LoC was conservative; the actual delta reflects D-E=α' (UI-stable hash), D-H pending visualization, and D-F=γ.1 instrument pin refinement, which were not in the original spike scope. F-5 invariant promoted to `SSoT_Runtime_CompositionSession_Integration §8` (D-K=α).
- **Action-card playability during composition loop — watch-item (opened 2026-05-12).** `_isSongPlaying` may not engage during active composition loop; the gate at `GigManager:1454-1462` may not enforce `AllowActionCardsDuringPerformance` as intended. Side-stepped today by `DiscardActionCardsOnPlay=true` (default). Worth verifying post-B1 if any "Action cards during performance" design returns to scope.
- **Phase B B2 — Polish layer (feedback + animation) — RESOLVED (2026-05-13).** Six items shipped monolithically per D3=A. Tooltip miniatures (#3), inspiration markers pulse (#4) + denied flash (D-Inspiration-Pool=A), expanded floating text (#5: composition events with diff-driven classifier D-FxChangeDetect=A + audience exclamations + Earworm multiplier-deferred), SongHype thresholds → venue SFX (#6), Robot beat-pop (#14), Worm stretch (#15), Worm instrument sub-animator (#16). See §1 closure block.
- **Phase B B2.5 — Polish refinements + cleanup — RESOLVED (2026-05-15).** 11 mandatory items shipped (correctness 1-3 + cleanup 7-11) plus item 16 (Tonality kept). 8 decisions locked (D-1 through D-8). Three preexisting bugs surfaced and 2 resolved within batch (D-7 ghost cards, D-8 macro-Vibe regression-and-fix); B3-cand-I (ParentActive=False warning during draws) captured for B3. Mid-batch D-5 fix introduced a regression (`_songHype` zeroed before `RunSongVibeResolution` could read it) caught by ST-B2.5-S6 diagnostic and surgically split via D-8. Hypothesis correction on the B2-era "Earworm tick lives in StatusEffectSO" assertion — code inspection showed only one tick site (the bespoke block in `GigManager`); fix was visual pacing, not relocation. Smoke tests S1, S2, S2b, S3, S4, S5, S6 all PASS. See §1 closure block.
  - **B2.5 deferred — content-dependent (3):** #4 per-venue smoke/fire VFX (art), #5 CompositionFxConfigSO default tuning (playtest), #6 animation feel tuning (playtest). Each blocked on its respective input; reopens when input lands.
  - **B2.5 deferred — design gaps (4):** #12 TempoScale diff in `SelectFxEntry`, #13 hasExplicit flags on PartEntry (mirror `hasExplicitRootNote`), #14 `PartActionKind.NoOp`, #15 `#if ALWTTT_DEV` gate on `DevAddSongHype`/`DevResetSongHype` **(RESOLVED in S5f 2026-07-04 — compile-gated, stripped from non-dev builds; ST-S5f-R4..R5 PASS)**. Carried to B3 candidate slate.
- **B3 candidate slate (accumulated during B2.5):**
  - **A. Mind Tap asset fix** — DONE in-batch (Earworm target switched from `AllAudienceCharacters` to `AudienceCharacter` per `Design_Audience_Status_v1.md §3.7`).
  - **B. AudienceMemberPosList reorder** — DONE in-batch (scene-level reorder to visual left-to-right; defensive sort by X-coordinate in `BuildAudience` not added — capturable if regression recurs).
  - **C. Effect-target-type authoring validation** (cross-effect target inconsistency warning in `CardEditorWindow`). Cost ~1h.
  - **D. CustomEditor for default Inspector showing effect labels** (mirror `BuildEffectLabel` from `CardEditorWindow` to surface in raw inspector). Cost ~1h. Low priority — Card Editor Window is the intended authoring path.
  - **E-lite. Blocked tooltip without icon.** Tooltip-only path to explain the "oscurito" sprite tint (Blocked → "immune to Vibe gains from this position"). Extend `AudienceCharacterCanvas.ShowContextual` or `OnPointerEnter` to surface contextual text. ~20-30 min. No status icon — Blocked is sprite-tint-only per M1.2 Decision E3. **RESOLVED in S5f (2026-07-04)** — sprite-hover tooltip "Bloqueado" (`AudienceCharacterCanvas.ShowBlockedTooltip`/`HideBlockedTooltip`), no icon, E3 intact; ST-S5f-R1..R3 PASS.
  - **F. Real `AudienceCharacterBase.ResolveLoopEffect` impl** — currently returns 0 (placeholder; B2.5 #9 cleanup removed the `clamped = 2;` forced-clamp that masked this). Without F, audience exclamations never fire, and per-audience macro-Vibe impression modifier stays at 1.0 (so all audiences get the same `SongHype01 × MaxVibeFromSongHype` Vibe). Content-blocker for audience expressiveness.
  - **G. Filter draws during composition session** (`OnCompositionLoopFinished` line ~1389). Per **D-B3-DrawFilter=B confirmed in B2.5**: composition cards + Always-action cards allowed; non-Always action cards excluded from per-loop draws. ~3-5 lines using existing `DrawCardFiltered` method.
  - **H. Always-action card discard semantics in SongPerformance start.** If Always-action cards become content (not in current starter), the `DiscardHandBetweenTurns` discard at `SongPerformance` phase start kills them before player can use them. Conditional discard would be needed. Not urgent until Always content exists.
  - **I. ParentActive=False warning during draws.** Preexisting bug surfaced in B2.5 logs: `DrawCards` warns `"Drew '...' but card GameObject is inactive. (ParentActive=False)"` for some early draws when `DrawTransform` parent isn't activated yet. `DrawCardFiltered` doesn't fire the warning (different ordering). Note in `GigManager:403` ("Ensure the hand is enabled BEFORE any card objects are instantiated by DrawCards") points to the right fix area. Investigation needed.
- **Phase B B3 — Content + design** (opened 2026-05-09, depends on B1). Aditivo. Inspiration cost/gen balance pass across deck — cover 0/1/2/3 for cost and generated (#9); rhythm composition cards with `+/-BPM` and `2×BPM` effects (#10); chord progression cards with key Modulation effect (#11); 1 designed audience member with 3 distinct abilities (#12). Audience Member Wizard Editor (#13) deferred post-demo per D4=B. B3 candidate slate above (A-I + design gaps #12-15) folds in during scoping.
- **F-2 D4 follow-up — `MaxInspiration` + `MaxCardsOnHand` to `GigFlowSettingsSO`** (opened 2026-05-08 per F-3 user feedback). Both fields currently live on `GameplayData` (separate SO) and `PersistentGameplayData`. Inconsistent with `DefaultInitialGigInspiration` and `DefaultInspirationPerLoop` which were consolidated to `GigFlowSettingsSO` in F-2. Post-demo priority — not gate-blocking.
- **M4.6F-4 SongOrchestrator IndexOutOfRange — STAGE A RESOLVED 2026-05-08, Stage B parked-until-natural-repro.** Stage A delivered: production-quality try-catch defense around `generator.Orchestrator.GenerateSinglePart` in `MidiMusicManager.RenderSinglePart` (+58 lines net); production-quality D3-B within-part recursion guard in `CompositionSession.HandleLoopFinished` mirroring `AdvanceToNextPart`'s `if (secs <= 0f) End();` pattern (+8 lines net); `[F-4]`-tagged diagnostic logs at both boundary sides (entry-log on call, full per-track + arg + stack-trace dump on catch). ST-F4-S1/S6 PASS; ST-F4-S3 PASS-vacuous; ST-F4-S2 DEFERRED-non-repro — IOOR did not surface this session; defense correctly silent (no exception thrown); no arg dump captured to route Stage B; Stage B reopens automatically if `[F-4][MMM]` LogError fires during playtest. ST-F4-S5 BLOCKED-OUT-OF-SCOPE — Player build fails on package-internal `MidiGenPlayConfig.GetChordWriteFolder` and `MidiGenPlayConfig.GetProfileForTonality` references inside `D:\Projects\MidiGenPlay\MidiGenPlay\Runtime\CoreScripts\Services\PatternRepositoryResources.cs:87` and `\Composition\SongOrchestrator.cs:142,326`; F-4 edits do not reference these methods; ALWTTT-side editor compile clean; tracked as separate MidiGenPlay-project batch. Defense + D3-B stay permanent; `[F-4]` diag logs strip at M4.6 demo closure (retroactive D5-C path) if no natural recurrence happens. See §1 closure block.
- **M4.6F-5 Composition next-loop pending workflow — ABSORBED into Phase B B1 (2026-05-09).** Originally opened 2026-05-06 with Lectura A confirmed (per-loop pending granularity, card played during loop N → resolves at start of loop N+1). During Phase B planning the user clarified that this behavior **already works** in the current zone (cards in current → replace track → effect at next loop). The complex piece — *next zone* (planning a future part) — is being simplified out, not implemented. B1 disables next zone, agrandar current zone to full-screen, model collapses to per-loop-only. F-5 is retroactively re-scoped; the deferred D2-A "TS transform mechanism" path remains explicitly post-Phase-B (could land if persistence proves valuable in playtest). Original code-name `Part` keeps current meaning; future Song Parts Library (planning/Design_Song_Parts_Library_v0_1.md) remains a long-term intent without forced rename pressure. See §1 Phase A close block.
- **Card Editor — Generic write-side support deferred** (opened 2026-05-06). JSON Import / Create Card / Add Existing / Sync targeting `GenericCardCatalogSO`. Touches `CardAssetFactory.CreateCardKindParams` and `MusicianCatalogService` contracts (both currently typed to `MusicianCardCatalogData`). Future tooling QoL batch.
- **Asset path layout cosmetic** (surfaced 2026-05-06). 10 new starter cards live under `Assets/Resources/Data/Characters/Musicians/starter_*.asset` rather than under `Robot_Cards/` or `Gusano_Cards/` subfolders. Side-effect of `CardAssetFactory`'s default output path resolution. Not functional; reorganization at user's discretion.
- **Conito catalog out-of-spec but inert** (surfaced 2026-05-06; **Cantante half resolved 2026-07-23, R1**). Conito (10/10 starter-flagged) is unchanged from pre-cleanup state and is not in the demo roster; cleanup lands with **R2**. Cantante was cleaned at R1 per D-R1-4=A: the 7 legacy entries were kept but set to `flags = None` (unreachable from both `IsStarter` assembly and `RewardPool ∩ UnlockedByDefault`, reversible, card assets preserved as reference), and **Wink** was added as the one starter-flagged entry. Remaining Cantante v2 slots populate at R3 / R6 / R8; reuse-vs-delete of the 7 parked entries is decided per slot at those batches. Tracked, not blocking.
- **`UnlockedByDefault` flag is editor-authoring-only (surfaced 2026-05-02, M4.6-prep batch (2) audit).** `CardAcquisitionFlags.UnlockedByDefault` has no runtime gameplay consumption today. Every reference is in editor code (Card Editor filter pills, validation warnings, JSON import validation, default value for new entries). Auto-assembly only consults `IsStarter`. The `UnlockedByDefault` + `unlockId` pair currently documents authorial intent for a future meta-progression / unlock system; no gameplay code reads them. Not a bug — flagged so future readers don't assume runtime enforcement that doesn't exist. Runtime consumption deferred to whenever a meta-progression batch lands.
- **Inventory viewer NRE on Draw/Discard/Hand pile open — RESOLVED (2026-05-02, M4.6-prep UI-fix-A).** `CardBase.SetCard` at `CardBase.cs:77` no longer throws because `CardUI.prefab`'s previously-unassigned `inspirationCostTextField` and `inspirationGenTextField` `[SerializeField]` refs are now wired. Asset-only fix on `CardUI.prefab`. `CardBase.SetCard` kept strict. See §1 UI-fix-A closure block.
- **`CardUI : CardBase {}` empty subclass — two-prefab arrangement (surfaced 2026-05-02, M4.6-prep UI-fix-A; appendix to batch (3) deferred 2026-05-03).** `CardUI` is a degenerate empty subclass of `CardBase` that exists solely to serve as a separate prefab GameObject's MonoBehaviour. The inventory canvas instantiates `CardUI.prefab` while gameplay instantiates the gameplay card prefab; both prefabs must independently wire every `[SerializeField]` field declared on `CardBase`. This is the recurrence vector for the UI-fix-A NRE class — any future TMP/Image field added to `CardBase` must be wired on both prefabs or the inventory side will NRE. Candidate cleanups (logged, not scheduled): (α) collapse to a single prefab with view-only mode driven by `SetCard(def, isPlayable=false)` — lowest drift risk; (β) make `CardUI.prefab` a Prefab Variant of the gameplay prefab so `CardBase` field additions inherit automatically — lower-risk migration than (α). Candidate appendix to batch (3) — "Validate `CardBase` prefab variants" Card Editor action that reflects over `[SerializeField]` fields and reports unwired refs at authoring time — was considered at batch (3) open and **explicitly deferred** (D3); logged in `SSoT_Editor_Authoring_Tools.md §14.5` as a candidate authoring-tool addition for a future QoL pass. S5b added three such fields — `actionBackground` + `compositionBackground` (GameObjects) + `ownerIconImage` (Image) — all null-guarded in `CardBase` and wired on both prefabs at close; latest instance of this recurrence vector.
- **Inventory scrollbar appears even with near-empty piles — paper cut (surfaced 2026-05-02, M4.6-prep UI-fix-B; ST-SCR-2 FAIL ACCEPTED).** `CardSpawnRoot` carries a fixed `LayoutElement.preferredHeight = 2050` so `Content` always reports overflow to `ScrollRect`, regardless of how many cards are actually displayed. Cosmetic only — does not affect functionality. Follow-up: replace the fixed value with a runtime computation in `InventoryCanvas.SetCards` based on active card count × grid params (`grid.cellSize.y`, `grid.spacing.y`, `grid.padding.top + grid.padding.bottom`, columns from `grid.constraintCount`). ~10 lines, computes `LayoutElement.preferredHeight` after population. Not blocking M4.6 demo.
- **FilterPanel scrolls with content (decision D-A deferred from M4.6-prep UI-fix-B, 2026-05-02).** `FilterPanel` lives inside `Content` under `VerticalLayoutGroup`, so it scrolls along with `CardSpawnRoot`/`SongSpawnRoot`. FilterPanel currently only contains TitleText (no functional filter chips), so scroll-with-content is harmless. Revisit when filters become functional: move FilterPanel out of `Content` and make it a sibling of `Scroll View` under `Midground` for sticky behavior.
- **Card Editor per-row starter UX — RESOLVED (2026-05-03, M4.6-prep batch (3)).** Batch (3.A) ships per-row `Starter` checkbox + `Copies` IntField columns on the catalog entry list, both via `SerializedObject` for Undo + dirty propagation parity with the right-side inspector. Batch (3.B) ships `CardInventoryWindow` (read-only viewer with Print + Export per view). Batch (3.C) ships toolbar Print buttons on Card Editor and Deck Editor. Smoke tests ST-AT3-1..8 all PASS. ST-AT3-8 dogfood acceptance confirmed the cleanup workflow is materially faster than the right-side inspector path. See §1 batch (3) closure block.
- **Pending Effects system (post-MVP, scheduled first).** Song-scoped accumulator layer where cards add to a pending bucket during a song and resolve at song end. First user: deferred Earworm. Mid-song multiplier cards become a content axis. Generalizes to pending Vibe / Stress / Flow / Cohesion. Does not affect M4.6 starter deck — Mind Tap and any other Earworm-applying starter card stay immediate-effect. Planning doc: `planning/Design_Pending_Effects_v1.md`. Implementation slot: first post-MVP gameplay batch immediately following M4.6 demo closure.

- **Tempo-coupled card identity (post-MVP, long-term, no implementation slot).** Design direction making tempo a gameplay input — cards prefer / require / shift tempo, producing fast-favoring vs slow-favoring deck identities ("metal" / "fast jazz" / etc.). Downstream of M4.6 closure, Pending Effects landing, and meter-stack playtest. No runtime commitment. Influences starter deck and per-musician catalog design now via flavor / naming / archetype lean — see `Design_Starter_Deck_v1.md` for tempo-lean notes per musician. Planning doc: `planning/Design_Tempo_Identity_v1.md`.

- **B3 audience pool authoring (opened 2026-05-15, expanded from #12).** Two archetypes shipped together: Cool Dude (3 abilities: Move One Step parameterized, Heckle composed `ApplyStatusEffect(exposed) + AddStress`, Indifference self-buff) + Kid (2 abilities: existing band-wide Stress, new `Egged On` buff on Cool Dude's outgoing Stress). New audience-side status `Indifference` requires implementing the deferred `ApplyIncomingVibe` helper on `AudienceCharacterStats` (mirror of M4.1 `ApplyIncomingStressWithComposure` pattern). Per D-DCP-6=A, Indifference blocks ALL incoming Vibe (song-end conversion + Earworm tick + direct ModifyVibe + future sources). Sound-design priority: Singing Field gains a per-card `InstrumentEffect` SO authored for Sibi (β path per D-DCP-5). Demo encounter (2×Kid + 1×CoolDude) authored as `GigEncounterSO`. SSoT edits required at closure: `SSoT_Status_Effects.md` new §5.8 Indifference; `SSoT_Audience_and_Reactions.md` §10 `ApplyIncomingVibe` canonical path + audience status pattern. See `Roadmap_ALWTTT.md §5.3`.

- **§5.3.5 Demo cut prep — RESOLVED (2026-05-18).** Demo build entry zero-clicks-from-launch-to-Gig via new two-scene Main Menu → Gig flow. Structural refactor extracts `GigLauncher` as the single non-Gig→Gig scene transition entry point; `MainMenuController` + `DemoLaunchConfigSO` enable auto-launch branching. SFX→FlatVibe mechanic shipped on stage crossings (per-audience via `ApplyIncomingVibe`, Indifference still blocks). Action-card mid-performance gate relaxed. `UIManager.Fade(false)` latent loop-termination bug fixed. F9 (B3-demo-polish ad-hoc precursor) replaced wholesale by the new architecture. ST-DCP-S5 win-rate validation deferred to §5.4 per DC-Close-S5=(c). See §1 closure block.

- **§5.3.5 ST-DCP-S5 win-rate validation — DEFERRED to §5.4 readiness review** (opened 2026-05-18). 8-10 playthrough win-rate validation against 60-80% target. Originally scoped to §5.3.5 closure; folded into §5.4's full clean-run smoke pass (R1 sub-item) per DC-Close-S5=(c). Three resolution paths still available at §5.4 close: (a) PASS at win-rate W% confirms §5.3.5 tuning baseline; (b) FAIL opens §5.3.5b tuning-only batch to adjust `initialGigInspiration` / `inspirationPerLoop` / `sfxBonusVibeStage{1,2,3}` on SO assets; (c) further deferral to post-demo if §5.4 scope grows.

- **§5.4 Demo readiness review (queued, opens after §5.3.5 closure applied).** Candidate sub-items per §3.2: full clean-run smoke pass (cold launch → win → exit → relaunch → loss → exit, absorbs ST-DCP-S5); polish-deferred items from prior batches (audience hover outline render, Kid Tantrum AnimatorTrigger consumption, Indifference + Hyped icon sprites); build pipeline (standalone builds produced + launched, no editor-only path leaks; MidiGenPlay Player-build errors may surface as gating, see post-demo follow-up); asset audit. Closes the demo cut tag; gates ladder mode opening.

- **Ladder mode (post-demo architectural batch, foundation laid 2026-05-18).** Foundation for multi-encounter ladder mode laid by §5.3.5's `GigLauncher` extraction. Will introduce: `LadderRunner` (`DontDestroyOnLoad`, encounter-queue holder, gig-won event subscriber); `EncounterLaunchConfigSO` family (DemoLaunchConfigSO sibling, per-encounter, designed for queuing); inter-encounter band carry-over via `bandRoster: null` to `GigLauncher.Launch`. Enables tuning of multi-gig mechanics: Cohesion, card rewards, deck modifications across encounters. Opens after §5.4 closes.

- **Pitch deck refresh (opened 2026-05-15, non-governance stream).** August 2025 pitch deck (`GoblinzStudio.pdf`) substantially obsolete (timeline window consumed, scope framing inflates aspirational features, team composition outdated, no demo screenshots). v2 batch scheduled post-§5.4 per PD-5=B; informal sub-batch A (audit + outline + draft text) can begin pre-§5.4 per PD-1=C interpretation α. Target deliverable PD-3=C (deck + video + playable build); minimum PD-3=B (deck + video). MidiGenPlay Player-build error follow-up may block PD-3=C build packaging — surfaceable risk. PD-4 commercial info captured: BCS Studios + Abstract Digital + CoverSolutions + Bamer29 partners; Claudio + Matías core team; EA 2027 / v1.0 2028 timeline; ~€200k ask; Cristian's Pretty Soon Games meeting (Digital Dragons 2026) immediate test case; Goblinz Publishing warm contact. See `Roadmap_ALWTTT.md §6`.

- **Sound design priority — standing design directive (declared 2026-05-15).** ALWTTT is a game about music bands; sound design is a maximum design priority that overrides convenience-of-authoring when in conflict. Operational consequences: per-musician instrument identity is preferred over reusing generic SOs (D-DCP-5=β path was chosen for Sibi's voice over reusing existing `Bass/Guitar/Synth`); future audience archetypes, statuses, and venues should consider audio identity at design time, not as a polish phase afterthought; PD-3=C build packaging is preferred over PD-3=B because the demo's core appeal is audible. Captured in new planning doc `planning/Design_Project_Directives_v0_1.md`. Should be considered for promotion into project-level instructions.

- **MGP-ALWTTT-MOD-DIR-1 (cross-project, non-blocking).** Directional modulation hint for `ChordTrackComposer` filed in MidiGenPlay project tracker. ALWTTT-side `ModulationEffect_KeyLift_Degree5.asset` will adopt the new `octaveHint` field when the package ships it. Current demo accepts non-directional voicing per D-MOD-DIR=A.

- **MGP-ALWTTT-SEED-1 (cross-project, resolved).** Filed → delivered → adopted 2026-07-05, same day. MidiGenPlay added a per-render seed parameter (`seedOverride`) to `GenerateSong`/`GenerateSinglePart`. ALWTTT owns the per-song seed *policy* (`SSoT_Runtime_CompositionSession_Integration.md §10`); MidiGenPlay keeps the selection mechanism. MidiGenPlay's own D4 (deterministic anti-repeat) was declined package-side; ALWTTT accepted probabilistic non-repetition with ≥6-entry palettes instead (`D-S5g-7=C`). Boundary entry: `SSoT_ALWTTT_MidiGenPlay_Boundary.md §8.1`.

- **Card cost-badge auto-hide at cost 0 (DEMO-FIXES-A, presentation).** `CardBase.SetCard` hides `inspirationCostBadgeRoot` when `InspirationCost == 0` (D-DF-5=A), mirroring the S5e-ext gen-badge hide. Two-prefab wired field (gameplay + `CardUI.prefab`). Prefab contract recorded in `SSoT_Card_System.md` §10.4; presentation tracked here alongside the gen-badge precedent until a presentation SSoT exists.
- **Dev Mode catalogue source moved to runtime band union (DEMO-FIXES-A).** The Catalogue tab now sources from `PersistentGameplayData.BuildBandCardCatalog` (band-scoped, no asset mutation, D-DF-7=A); `GameplayData.AllCardsList` is demoted to fallback-only and deprecated as a hand-maintained catalogue. Home: `SSoT_Dev_Mode.md` Catalogue tab.
- **Audience intention telegraph shows placeholder ability names (surfaced during DEMO-FIXES-A, out-of-batch).** The floating "project effect ×N" over audience members is `AudienceAbilityData.abilityName` (authored placeholder) rendered by the intention telegraph (`AudienceCharacterBase.ShowNextAbility` → `AudienceCharacterCanvas`; `×N` = `ActionList[0].ActionValue`). Not a code literal — content cleanup on the demo audience `AudienceCharacterData` assets (Kid / Cool Dude), or a telegraph-name toggle in `AudienceCharacterCanvas`. Candidate for a DEMO-FIXES content item or the S5i content pass. `AudienceCharacterCanvas.cs` not in the governed set — add before a code-level toggle.

- **Centinela `TempoRange.Fast` en la rama `AbsoluteBpm` de `TempoEffect` (CTX-2a, 2026-08-03 — anotado, inerte).** `SongCompositionUI.ApplyEffectToModel` escribe `tempoRangeOverride = TempoRange.Fast` al aplicar una carta de BPM absoluto. Tras D11=A ese centinela contradice el default del modelo. Es inerte mientras `ExplicitBpm` tenga valor (lo ensombrece — §8 inv 13); se vuelve visible solo si alguien limpia el `ExplicitBpm` de esa parte sin limpiar el rango. Arreglo de una línea, sin urgencia. Home: `SSoT_Dev_Mode.md` §18.13 (deuda anotada).

- **D-ARTIC-AUDIT — qué hacer con los bundles ya autorados con `arpeggioRate = Random`
  (CTX-2b, 2026-08-03 — ABIERTA).** F-ARTIC-RATE-RANDOM-1 hace que esos bundles pierdan
  su articulación en silencio. **(A)** no hacer nada: MGP-ARTIC-RATE-1 lo arregla y el
  problema desaparece — corregir assets contra un bug temporal es trabajo que habrá que
  deshacer. **(B)** corregir los assets ahora poniendo un rate concreto. **(C)** B + regla
  en el contrato de autoría de cartas. **Recomendación: A**, con la salvedad de **contar
  cuántos bundles están afectados** (búsqueda corta): si son muchos, la demo suena peor de
  lo que debería ahora mismo y el cálculo cambia. Escribirlo en un contrato de autoría
  (C) convertiría un fallo temporal en regla permanente. Home: este §4 hasta que MGP
  responda.

- **Wormus Major / Wormus Minor ahora fijan la tonalidad de la parte (2026-08-05 — cambio de baseline de S5i).** Se añadió un `ModifierEffect` de tonalidad a ambas cartas (`TonalityEffect_Ionian` y `TonalityEffect_Aeolian`). Motivo: sin él, la escala de la parte quedaba en Ionian mientras los acordes eran menores, y toda melodía procedural generaba contra una escala que no correspondía a la armonía. El composer ya lo señalaba en log (`authored for [Aeolian] but part is Ionian; rendering AsAuthored`). Afecta a contenido de demo (Sibi): no-regresión verificada por escucha (ST-R3-12 re-corrido, PASS). **No resuelve el caso modal:** una paleta con entradas de varios modos no puede expresarse con un `TonalityEffect` fijo — eso es lo que resuelve **JAM-2** en el plano de render (R3, 2026-08-08), no en el modelo. Ver ask MGP-MEL-1 P4.

- **D-R4-1 — ¿el público juzga la tonalidad AUTORADA o la que SUENA? (R3, 2026-08-08 — ABIERTA, owed antes o durante R4).** `LoopFeedbackContext` se construye desde el **modelo de UI**, así que la evaluación de gustos de la audiencia lee la tonalidad autorada, no la sonante. Bajo armonía modal —donde JAM-2 alinea el render pero deliberadamente **no** toca el modelo— la audiencia ve Ionian aunque suene Lidio. **Es una pregunta de diseño, no un defecto**, y por eso no se resolvió dentro de R3: la respuesta cambia qué significa el gusto de tonalidad como eje de juego. **(A)** el público juzga lo escrito — barato, coherente con que la carta declare su intención, pero el jugador puede oír algo que la audiencia no está evaluando. **(B)** el público juzga lo que suena — intuitivo para el jugador, pero exige llevar la tonalidad de render hasta `LoopFeedbackContext`, que hoy no la ve. **Sin recomendación**: depende de si el gusto de tonalidad debe premiar la decisión del jugador o el resultado audible, y eso es diseño de juego. Home: este §4 + `planning/active/RosterExpansion_Sub_Roadmap.md` §8. Límite técnico documentado en `SSoT_Runtime_CompositionSession_Integration.md` §13.3.

### Residual risks
- **GigManager flag lifecycle surveillance:** `_isSongPlaying` was not observed to drift but a symmetric single-use-per-gig pattern may exist elsewhere. Low-priority audit recommended.
- **Status icon animation pause behavior:** icon animations use `Time.deltaTime`. If a future pause feature sets `Time.timeScale = 0`, icon popups freeze. Switch to `Time.unscaledDeltaTime` if pause-transparent animations become desired.
- **Composition face minimal display:** the shortened face only shows role/part + modifier count. M1.10 detail modal now provides full inspection. Cosmetic items remain: "COMPOSITION" word-break on narrow panels, panel overflow on cards with many modifiers. Neither blocks gameplay testing.
- **M4 roster reduction (2-musician starter) intentionally narrows MVP demo (2026-04-21 design decision):** starter band is C2 + Sibi only. Conito and Ziggy deferred to post-MVP roster expansion. Demo will show a band that is smaller than the final design; this is deliberate and scoped to reduce art and tuning cost. Documented in `planning/Design_Starter_Deck_v1.md`.

- **`ApplyIncomingVibe` deferred helper:** the audience-side equivalent of `ApplyIncomingStressWithComposure`. Not implemented in MVP because Earworm (the only audience status in the starter) does not modify incoming Vibe; it generates Vibe on tick. Hook point identified and documented in `planning/Design_Audience_Status_v1.md` for when Captivated lands with Ziggy.

---

## 5. Docs that must be edited next

The §5 doc-closure *history* (every applied edit since 2026-04-29) was removed in the 2026-06-16 hygiene prune — that record lives in `changelog-ssot.md`. This section is now forward-looking only.

- **Applied 2026-06-16 (TUT-JAM-SEQ):** `Design_Tutorial_System_v0_1.md` (new §6A jam sequence + reframe S2→S4 corrections), `Design_Demo_Cut_v1.md` §2.4 (+2 coverage rows), `changelog-ssot.md` (entry). Planning-only — no SSoT / authority / contract change.
- **Owed by S4 (when it lands):** `Design_Tutorial_System §6A` status (design → shipped); new bus-event rows in `Design_Sensory_Contract §3` (`CardPlayedEvent` + Inspiration-delta + per-loop-gain); `SSoT_Editor_Authoring_Tools.md` or a promoted tutorial SSoT if `TutorialController` earns one; `changelog-ssot.md`; `coverage-matrix.md` + `SSoT_INDEX.md` only if a tutorial authority is promoted or navigation changes.
- **Applied 2026-07-13 (JUICE-PW-DOC session):** the JUICE-PW doc pass (`JUICE-PW_Doc_Diffs_2026-07-13.md`, 19 diffs across 8 docs, + OPT-1/OPT-2 backfills) **and** the stacked S5h doc pass (`S5h_Doc_Diffs_2026-07-07.md`, 7 of 8 diffs — DIFF-S5h-6 skipped per its own provisional clause, superseded by TUT-REBUILD). Both diff packages retired from the PK. Also registered the expanded DEMO-FIXES backlog (planning-only, sub-roadmap).
- **Applied 2026-07-16 (BALANCE-XREF, docs-only balance-research consolidation):** new `planning/Design_Game_And_Card_Maxims_v0_1.md` (maxims doc, reference/design-philosophy); `Design_Starter_Deck_v1.md` v1.4 (uniqueness-ratio correction + maxims cross-ref); `S5_DemoCutClose_Sub_Roadmap.md` (BALANCE-XREF ledger BR-D1..4, TLM-1 batch, S5i lenses); `Design_Vertical_Slice_v0_1.md` (§3.1 reward-skip + power curve, §11 BR-D3 trigger, §12 cross-refs); `CURRENT_STATE.md` (§2/§3/§4/§5); `changelog-ssot.md` (entry). No SSoT / authority / contract change.
- **Owed by BALANCE-XREF (partially applied):** the maxims-doc registration row in `coverage-matrix.md` was **applied 2026-07-16 (TLM-1 close sweep)**. **Saldado 2026-08-08 (MANIFEST-1):** el doc de maxims queda registrado en `SSoT_INDEX.md` y en `ssot_manifest.yaml` como `reference` (design philosophy), tal como se preveía.
- **Applied 2026-07-16 (TLM-1 close):** `SSoT_Dev_Mode.md` (new §17 run-logger surface + §9.12 smoke rows + update-rule bullet); `CURRENT_STATE.md` §2/§3/§4/§5; `changelog-ssot.md` (entry); `coverage-matrix.md` (maxims row — BALANCE-XREF sweep); `S5_DemoCutClose_Sub_Roadmap.md` (TLM-1 closed + TLM-1b rider). No SSoT authority/contract change beyond the new Dev Mode §17 surface.
- **Applied 2026-07-20 (CSV-4 doc pass, documentation-only):** `SSoT_ALWTTT_MidiGenPlay_Boundary.md` (§4.3 D-BAG-3/MGP-MIX-1 registered; new §8.2 MGP-BAGGAGE-1 + Poly Synth rider); `CSV_Composition_Validation_Sub_Roadmap.md` (CR-10; decision ledger D-CSV-7/15/16/18/19/21/22/23 moved to locked, D-MEL-1 opened, D-CSV-14 reduced; **§4.1.1 replaced wholesale**; batch table + CSV-4b row + blocking note; §5 asks recalibrated); `SSoT_Editor_Authoring_Tools.md` (§17.2 read-only reaffirmed, §17.6 `sample` origin, §17.7 D-CSV-14 rider, §17.10 escalation, new §17.11 pool-level instrument curation); `SSoT_Runtime_CompositionSession_Integration.md` (new §12 part-field resolution); new `planning/Design_Asset_Naming_v0_1.md`; `SSoT_INDEX.md` + `coverage-matrix.md` (registration); `CURRENT_STATE.md` §1/§2/§4/§5; `changelog-ssot.md`. No code change, no smoke tests (documentation-only batch).
- **Owed next:** DEMO-FIXES close docs (tutorial §9.2 for R1; `SSoT_Gig_Combat_Core` §14 rider if DF-ECONTIP lands; authoring contract §9 extension if DF-INSPLOOP lands); the **D-SENSORY-HOME** decision (§4); `ssot_manifest.yaml` invariant-18 hardening — **APLICADO 2026-08-08 (MANIFEST-1)**: el fragmento OPT-5 («una carta suena una vez, por exactamente un camino») está levantado literalmente de `SSoT_Audio §7` inv 18 a los `hard_invariants` de la entrada de SSoT_Audio; boundary-doc entry for **MGP-ALWTTT-ARTIC-1** — **saldado 2026-08-03** (§8.7 del boundary SSoT; el ask ya estaba entregado package-side desde el snapshot MGP-20260729, la entrada estaba pendiente por deriva de registro, no por falta de entrega). Nuevo ask abierto en su lugar: **MGP-ARTIC-RATE-1** (F-ARTIC-RATE-RANDOM-1), filtrado 2026-08-03, sesión paralela en el proyecto MidiGenPlay.
- **Applied 2026-07-31 (DOC-APPLY-1 — stacked doc session):** the five pending packages applied in order R0 → R1 → R2(+R2c/R2d) → CONT-B(+CTX-1/1b) → AUTH-1(+1b): `RosterExpansion_R0_Doc_Diffs_2026-07-23.md` (P1–P9; P2.9 partial — the R1 rehydration prompt text was not preserved and is recorded as superseded, not reconstructed) · `RosterExpansion_R1_Doc_Diffs_2026-07-23.md` (17 diffs; DIFF-R1-14 variant (a)) · `RosterExpansion_R2_Doc_Diffs_2026-07-31.md` (R2-1..R2-10 applied; **R2-11 deferred** — `coverage-matrix.md` has no per-batch smoke-suite section; smoke record preserved in the changelog entry) · `CONT-B_Doc_Diffs_2026-07-31.md` (§1–§9 incl. the two formerly-unconfirmed anchors, both verified: `SSoT_Audio.md` §4.6 and `Design_Asset_Naming_v0_1.md` §3) · `AUTH-1_Doc_Diffs_2026-07-31.md` (1a–1i + 2 + 3 + 4; 1i applied after R2, retiring R2 §0.5). All five diff files **retired from the PK on apply** (package convention; the 2026-07-30 R2 draft was already superseded and was never in the PK). Also applied: the **D-S5e-DOC-D** convince-condition sweep (D-DOC-A4(i)) and the **D-SEQ-3=A** R4+-gate amendment (snapshot tag, not S5j). **Still owed:** `CONT-B_Returns_MidiGenPlay_2026-07-31.md` (referenced by the CONT-B changelog entry; cross-project deliverable, not yet produced — the CSV §5 rows and D9 point at it); R2-11 smoke-record disposition — **RESUELTA 2026-08-08 (MANIFEST-1, D8)**: el registro de un smoke vive en la SSoT dueña del observable, el changelog registra el resultado del lote, y `coverage-matrix.md` no crece una sección de smokes. Regla escrita en `SSoT_CONTRACTS.md §8`; `Design_Asset_Naming_v0_1.md` §3-vs-§4 schema reconciliation (CSV-4b); the pre-existing **CSV-3-DOC** doc-pass (explicitly out of DOC-APPLY-1 scope, D-DOC-A4(ii)).

- **Pendiente de aplicar (CTX-2a, propuesto 2026-08-03):** `CTX-2a_Doc_Diffs_2026-08-03.md` — 10 diffs sobre `SSoT_Dev_Mode.md` (§18.13 nueva · §9.18 nueva · lista §18), `SSoT_Runtime_CompositionSession_Integration.md` (§8 **invariante 13 nuevo** — resolución/cacheo consumer-side del BPM · §12.4 nueva — default de tempo del modelo), `CURRENT_STATE.md` (§1/§4/§5), `changelog-ssot.md` y `Design_Composition_Debug_Tab_v0_1.md` (§3.3 filas 5–6 · §8 D4). Código entregado y ST-CTX2A-1..7 PASS. **Sin cambios en `SSoT_INDEX.md`, `coverage-matrix.md` ni `ssot_manifest.yaml`**: no se crea documento nuevo ni se mueve autoridad. Retirar el paquete al aplicar.
- **Owed post-R3:** la pasada de medición de **D11b** (§4) y, con su resultado, el ajuste de las bandas de `TempoRange` en el proyecto **MidiGenPlay** (package-side, lote corto). Hasta entonces `Slow` queda como está.
- **Aplicado 2026-08-08 (MANIFEST-1, documentación pura, sin código ni smokes).** `ssot_manifest.yaml` reescrito y, por primera vez, **validado contra un export del árbol real del repo** (3.251 rutas): fecha 2026-04-14 → 2026-08-08; altas de `SSoT_Dev_Mode` (12 invariantes, incluidas las siete líneas de log protegidas de §19), `SSoT_Singer_Voice` (8), `Design_Composition_Debug_Tab_v0_1`, `Design_Game_And_Card_Maxims_v0_1`, `Design_Tutorial_System_v0_2`, `Design_Starter_Deck_v1` (archive), `Design_Tutorial_System_v0_1` (archive) y `CONTRIBUTING.md` (excluded); `changelog-ssot` reclasificado `archive` → `reference` (D5=A); invariante OPT-5 aplicado; cinco rutas corregidas contra el árbol. **Cinco de los siete hallazgos del auditor de DOC-APPLY-2 cierran**, y las tres contradicciones de autoridad que el árbol destapó se resolvieron en el mismo pase: **F17** (dos documentos de tutorial vivos → v0_1 archivado, v0_2 promovido a `planning/active/`, D11=A), **F18** (`Design_Starter_Deck_v1` en `archive/` declarándose autoritativo → la carpeta era correcta y la palabra era falsa, D10=B), **F14** (`Roadmap_ALWTTT_Debug_Seams.md` no existe → fila retirada del índice, D12). Docs editados: `ssot_manifest.yaml` · `SSoT_INDEX.md` · `coverage-matrix.md` · `SSoT_CONTRACTS.md §8` · `SSoT_Dev_Mode.md` (§18.11 + dos rutas) · `SSoT_Singer_Voice.md` §3.6 · `SSoT_ALWTTT_MidiGenPlay_Boundary.md` §8 · `SSoT_Editor_Authoring_Tools.md` §17.12 · `CSV_Composition_Validation_Sub_Roadmap.md` §4.1.1 · los dos docs de tutorial · los dos de starter deck · este fichero · `changelog-ssot.md`.
- **Owed a MANIFEST-2 (después de R4):** la señal **F11** (~17 documentos indexados que el manifiesto aún no enumera) junto con **D-SENSORY-HOME**; y la señal **F19** (ver abajo). **F16 quedó casi cerrada el mismo día** con confirmación del usuario: `GigSetupSceneManager.cs` fue **borrado el 2026-05-18** —consta en las notas de cierre de D-FAST-1=C del `Roadmap_ALWTTT`, el manifiesto llevaba tres meses gobernando un fichero inexistente—; `reference/Report_CardLLM_Pipeline.md` **ya no existe** y su entrada queda como lápida, con la consecuencia registrada de que **el mecanismo del pipeline LLM se queda sin hogar documental** (solo código); y los dos documentos del POC de Pink Trombone **pertenecen al proyecto MidiGenPlay**. Queda un solo residuo real: la carpeta `Docs/planning/music/` no existe, así que el informe de emulación de soundfont (2026-03-24) no tiene hogar — se resuelve con un `ls` y o bien una corrección de ruta o un borrado con línea de changelog.
- **F19 / D13 — RESUELTA 2026-08-08 = A.** `PinkTrombone_Voice_Levers.md` **promovido a `Docs/reference/` de ALWTTT** y registrado en el manifiesto y en `SSoT_INDEX`. Es la fuente del esquema de `VoiceProfileSO`, un asset propiedad de ALWTTT (`Assets/Scripts/Data/Audio/VoiceProfileSO.cs`), y su propia cabecera ya autorizaba el movimiento: "viaja con el cantante cuando se promueve a ALWTTT" — y el cantante se promovió en SINGER-1 (2026-07-21). No es un interno del paquete: son seis levers de diseño de voz consumer-side, que la regla de frontera no excluye. **Lo que cierra esto en concreto:** con el esquema al otro lado de la frontera, una edición de los levers en MidiGenPlay habría desviado `VoiceProfileSO` sin que ninguna auditoría lo viera, porque el auditor no cruza la frontera. `PinkTrombone_Rendering_POC_Verdict.md` se queda en MidiGenPlay como research.
- **Ciclo de vida del fork (F16, residuo).** Los dos documentos del POC confirman por escrito lo que decía el árbol: el fork vive dentro de `Assets/PinkTrombonePOC/PinkTromboneSrc/`. **La promoción a `Assets/ThirdParty/PinkTrombone/` que §3.6 y §7 de Singer Voice describían nunca se ejecutó.** El invariante se sostiene en sustancia —consumer-side, nunca en `MidiGenPlay.Runtime`— y §3.6/§7 quedan corregidas. Lo abierto no es documental: hay que decidir si se quiere la promoción **antes** de retirar el arnés, porque el código de producción del cantante depende hoy de una carpeta llamada "POC".
- **Owed:** el conteo de inventario quedó re-baselinado a **232 assets** (2026-08-08) y **D-CSV-14 es cerrable** (los únicos `OFF-ROOT` locales son los dos patrones de melodía bajo `Patterns/Melody` singular). El índice inverso carta→bundle (**D-CSV-16**, bloqueante desde CSV-4) recibe por fin lote: **CSV-4c**, que además añade la vista de cartas de Bassline.
- **Low-priority pending (optional, unscheduled):** `SSoT_Gig_Combat_Core.md §4.2` — one-line note on Inspiration dual-siting (PD vs the session's live budget), surfaced by M1.5 P3.2. Not scheduled.

---

## 6. Working rule

`CURRENT_STATE.md` answers:
- what is the project foundation
- what is active now
- what comes next
- what is blocked or at risk
- which docs need editing next

It does **not** replace subsystem SSoTs.
